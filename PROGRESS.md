# FBR Admin — Estado do Projecto

> Este ficheiro é actualizado no fim de cada sessão de trabalho.
> No início de cada sessão, lê este ficheiro primeiro para retomar exactamente onde ficámos.

---

## Fase actual: FASE 6 (parte 7) — Tabela de preços + 3 abas placeholder + audit log UI + refactor

### Fases do projecto
- [x] **Fase 1** — Fundação: Supabase ligado, autenticação, layout/navegação ✅
- [x] **Fase 2** — Preservação de Flores: tabela, workbench, estados, orçamento, permissões ✅
- [x] **Fase 3** — Vale-Presente (admin + site público voucher.floresabeirario.pt) + Status ✅
- [x] **Fase 4** — Dashboard + Tarefas + Métricas ✅
- [x] **Fase 5** — Formulários públicos + Parcerias ✅
- [~] **Fase 5.5** — Afinações pós-uso (parte 1 ✅; parte 2 quase fechada — falta `fbr-website`)
- [~] **Fase 6** — Integrações + PWA + RGPD completo ← **EM CURSO (parte 1: status público)**

---

## O que está feito
- [x] Projecto Next.js com shadcn/ui
- [x] Fontes da marca: Tan Memories + Google Sans
- [x] CLAUDE.md com spec completa da plataforma
- [x] Supabase ligado (`src/lib/supabase/client.ts` + `src/lib/supabase/server.ts`)
- [x] `.env.local` + variáveis no Vercel
- [x] `src/proxy.ts` — protecção de rotas
- [x] Login estilo Netflix com fotos (António, MJ, Ana) — `src/app/login/page.tsx`
  - Fotos em `public/userphotos/`
  - Emails: info+antonio / info+mj / info+ana @floresabeirario.pt
  - António e MJ = admin | Ana = viewer (permissões a implementar na Fase 2)
- [x] 3 utilizadores criados no Supabase (email + password)
- [x] `src/app/(admin)/layout.tsx` — sidebar colapsável com 10 abas
- [x] Dashboard + páginas placeholder para todas as abas
- [x] Deploy no Vercel a funcionar em fbr-admin2.vercel.app

## O que está a fazer (em curso)
- **Sessão 47 ✅ Redesign Entregas e Recolhas + fix "Atrasadas" + Mapa + Notas de recolha.**

  **Parte 4 — Notas internas sobre a recolha:** novo campo `pickup_notes TEXT` em [orders](supabase/migrations/031_pickup_notes.sql) (migration 031) — texto livre destinado a quem vai fisicamente fazer a recolha (ex: "é o pai da noiva que vai entregar as flores", "tocar à campainha 3 vezes"). Distinto de `additional_notes` (cliente) e `sticky_note` (post-it geral). **Workbench:** novo Field "Notas para quem vai fazer a recolha" como Textarea de 2 linhas dentro da caixa violeta "Detalhes da recolha" (só aparece quando `flower_delivery_method=recolha_evento`), [preservacao/[id]/workbench-client.tsx](src/app/(admin)/preservacao/%5Bid%5D/workbench-client.tsx). **Página Entregas e Recolhas:** novo helper `<PickupNotesBox>` com fundo amarelo (`bg-amber-50` + `border-amber-300`), ícone `StickyNote`, suporta variante `compact`. Aparece em destaque no `TodayCard` (depois do badge de tipo) e nas linhas normais `LogisticsRow` (depois do contexto do evento). **Mapa:** `MapMarker` ganha campo `notes`; `renderInfoWindow` mostra-as como bloco amarelo inline `📝 …` antes do label de tipo. Type check + ESLint limpos.

  **Maria precisa de fazer manualmente:**
  1. Correr migration 031 no Supabase Studio: SQL Editor → cola conteúdo de [031_pickup_notes.sql](supabase/migrations/031_pickup_notes.sql) → Run
  2. Push para Vercel
  3. Smoke test: encomenda com `flower_delivery_method=recolha_evento` — workbench mostra textarea "Notas para quem vai fazer a recolha" dentro da caixa violeta; escrever uma nota e guardar; ir a Entregas e Recolhas → vê caixa amarela com a nota no card/linha + no popup do mapa

- **Sessão 47 (parte 3) ✅ Redesign Entregas e Recolhas + fix "Atrasadas" + Mapa interactivo.**

  **Parte 3 — Mapa interactivo de recolhas/envios:** novo componente [logistics-map.tsx](src/app/(admin)/entregas-recolhas/logistics-map.tsx) que adiciona coluna direita sticky em desktop (`lg:col-span-2` para a lista, `lg:sticky lg:top-4` para o mapa) com mapa Google Maps mostrando marcadores das recolhas/envios pendentes. **Geocoding ao vivo + cache:** usa Google Geocoding API (lib `geocoding` via `@googlemaps/js-api-loader`) para traduzir cada `pickup_address`/`event_location` em coordenadas; resultado guardado em `localStorage` (key `fbr-geocode-cache-v1`) por string de endereço — cada morada só geocoda 1x por browser. **Custos:** dentro do crédito grátis de $200/mês do Google Maps (estimativa: <30 chamadas únicas por sessão). **Marcadores** circulares coloridos por proximidade temporal via helper `bucketStyle(date)`: vermelho `#dc2626` atrasada, verde `#10b981` hoje, âmbar `#f59e0b` amanhã, azul `#3b82f6` esta semana, cinza `#6b7280` mais tarde. **InfoWindow** ao clicar num marcador (HTML escapado via `escapeHtml`) com nome+orderRef+data+hora+localização+tipo+link "Abrir encomenda →". **Fitting:** `map.fitBounds(bounds, 48)` se >1 marcador, `setZoom(13)` se apenas 1, default center Portugal continental `{ lat: 39.5, lng: -8.0 }` zoom 7. **Filtro independente** do mapa via novo segmented control `<MapFilterToggle>`: `Recolhas` (default) · `CTT` · `Ambos`; estado `mapFilter` separado do `kindFilter` das KPI cards, computa `mapMarkers` filtrando completed=false + tipo. **Fallback gracioso:** sem `NEXT_PUBLIC_GOOGLE_MAPS_KEY` mostra placeholder com instruções; erro de carregamento mostra mensagem amarela italic; loading state com `Loader2` spinner; estado vazio mostra "Nenhum item para mostrar no mapa." **Legenda** abaixo do mapa com `LegendDot` para cada cor + contagem de endereços sem coordenadas válidas. Type check + ESLint limpos.

  **Maria precisa de fazer manualmente:**
  1. Garante que `NEXT_PUBLIC_GOOGLE_MAPS_KEY` está configurada (já estava da Sessão 46 Lote D — se sim, mapa funciona logo)
  2. Push para Vercel
  3. Smoke test: entrar em /preservacao/entregas-recolhas — mapa aparece à direita em desktop com pontos coloridos; clicar num ponto abre popup com info da encomenda; toggle "Recolhas / CTT / Ambos" muda o que aparece; primeira vez demora 1-2s por morada (geocoding), depois fica em cache no browser

- **Sessão 47 (parte 1-2) ✅ Redesign da página Entregas e Recolhas + fix da lógica de "Atrasadas".** Maria observou que a aba "Atrasadas" listava recolhas cujo estado da encomenda já era `flores_recebidas` ou posterior — ou seja, a recolha foi feita com sucesso, não é atrasada. **Fix lógico:** introduzido `isCompleted(item)` em [entregas-recolhas-client.tsx](src/app/(admin)/entregas-recolhas/entregas-recolhas-client.tsx) que classifica cada `LogisticsItem` como concluído quando: (a) `recolha_evento` ou `envio_ctt_flores` e status ∈ `FLOWERS_RECEIVED_OR_LATER` (12 estados de `flores_recebidas` até `quadro_recebido`); (b) `envio_ctt_quadro` e status = `quadro_recebido`. Itens concluídos saem dos buckets temporais e vão para um bloco "Concluídas" colapsável (default fechado), com `opacity-70`. **Redesign focado em quem vai fazer a recolha/entrega:** a página deixou de ser "buckets temporais arbitrários" (próximos 7/30 dias) e passou a **agenda** com (1) **HOJE** em destaque (border-2 emerald, gradient verde, cards individuais grandes com hora em coluna esquerda 2xl bold + localização clicável + cliente + tipo de evento); (2) **Amanhã** (warning tone, dia da semana no subtítulo); (3) **Esta semana / Próxima semana / Mais tarde** com prop `groupByDate` que agrupa por dia dentro da secção, header pequeno "Segunda · 18/05/2026" antes dos itens desse dia; (4) **Atrasadas** danger tone — só itens NÃO concluídos no passado. **Dia da semana visível** em todos os headers de data (format `"EEEE · dd/MM/yyyy"` com locale `pt`, `capitalize` no CSS). **Data do evento como contexto:** quando `recolha_evento` e `event_date !== pickup_date`, mostra "Evento: sábado, 16/05/2026" em texto pequeno italic (caso o evento tenha sido sábado mas a recolha agendada para segunda). **Localização clicável** abre Google Maps (`https://www.google.com/maps/search/?api=1&query=...`) em nova tab — facilita navegação no telemóvel. **`getAllLogistics`** também ordena por `timeFrom` dentro da mesma data (não só `date`). **Date.now() impure fix:** usar `addDays(new Date(), 1)` dentro de `useMemo` em vez de `Date.now() + 86400_000` (eslint `react-hooks/purity` flagueia `Date.now()` mas aceita `new Date()`). Type check + ESLint limpos.

  **Maria precisa de fazer manualmente quando aceitar este lote:**
  1. Não há migração nem env vars
  2. Push para Vercel (auto-deploy via main)
  3. Smoke test:
     - Entregas e Recolhas: HOJE em destaque verde; recolhas em estado `flores_recebidas` ou posterior deixaram de aparecer em "Atrasadas" — aparecem agora em "Concluídas" (bloco colapsável no fundo)
     - Clicar na localização abre Google Maps em nova tab
     - Para encomenda com `pickup_date` ≠ `event_date`: deve ver linha "Evento: <dia>, dd/MM/yyyy" como contexto

- **Sessão 46 ✅ Afinações pós-uso: sidebar+workbench (Lote A) + calendário multi-vista (Lote B) + consolidação "Sistema" (Lote C) + Google Maps Places autocomplete (Lote D).** Pacote grande pedido pela Maria de uma vez. **Lote A (UX rápidos):** (1) `Entregas e Recolhas` passou a sub-aba de Preservação no sidebar (`parent: "/preservacao"`) — fica agrupada com Status; (2) `"FBR Admin"` no sidebar (expandido e colapsado-F) e na top-bar mobile agora é `<Link href="/">` para o Dashboard; (3) Bandeira PT/EN no header do card "Comunicações" do workbench passou a botão clicável — alterna `form_language` entre `pt` e `en` com diálogo de confirmação via `clientUpdate` (porque é campo preenchido pelo cliente, evita cliques acidentais); (4) `Inventário das flores` saiu do card central "Flores, quadro e extras" e ganhou Card própria na coluna esquerda do workbench, com badge de contagem, accent `green`. **Lote B (calendário Semana/Mês/Ano + ícones logísticos + popover):** [calendar-view.tsx](src/app/(admin)/preservacao/calendar-view.tsx) reescrito. **Switcher** segmented "Semana · Mês · Ano" no header, navegação prev/next adapta-se ao modo, título contextual ("3 – 9 de Junho 2026" em semana, "Junho de 2026" em mês, "2026" em ano). **MonthView** mantém grelha Seg-Dom com 3 eventos visíveis + "+N mais" expansível (refactor do existente). **WeekView** nova: 7 colunas grandes (min-h-260px) com dia destacado + lista expandida de eventos por dia, variante `"detailed"` do chip mostra tipo de evento inline. **YearView** nova: 12 mini-meses lado-a-lado em grelha responsive (cols 2/3/4), cada mini-mês com nome+contagem+grelha 7×N de quadradinhos; cor do quadrado vem de `heatmapClass(count, maxPerDay)` — escala rose-100/200/400/600 conforme intensidade relativa; click no mini-mês muda para vista mensal desse mês. **`deliveryBadge(o)`** devolve `Truck` rose-600 para `flower_delivery_method=recolha_evento`, `Hand` sky-600 para `=maos`, null para CTT/não sei (não acionável). Ícone aparece antes do nome em ambas as vistas. **Popover** ao clicar num evento (substitui o `onClick→onOpenOrder` directo) — `<EventPopoverBody>` mostra nome+tipo, badge de estado, email/WhatsApp clicáveis, localização, e quando é recolha um bloco rose com `pickup_address`/`pickup_time_from-to` (placeholder italic se faltam dados); botão "Abrir encomenda" no fim. Legenda inferior ganha ícones logísticos (Recolha + Entrega em mãos). **Lote C ("Sistema"):** novo componente partilhado [src/components/sistema-topbar.tsx](src/components/sistema-topbar.tsx) com pill-tabs (`Definições` · `RGPD` · `Histórico` · `Healthchecks` · `Ecossistema`) usando `pathname.startsWith` para o estado activo; filtra adminOnly conforme prop `isAdmin`. Adicionado ao topo dos 5 client/server components existentes ([settings-client.tsx](src/app/(admin)/settings/google/settings-client.tsx), [rgpd-client.tsx](src/app/(admin)/settings/rgpd/rgpd-client.tsx), [audit-client.tsx](src/app/(admin)/settings/audit/audit-client.tsx), [healthchecks-client.tsx](src/app/(admin)/healthchecks/healthchecks-client.tsx), [ecossistema/page.tsx](src/app/(admin)/ecossistema/page.tsx) — esta passou a async para chamar `getCurrentRole()`). URLs antigas continuam a funcionar. **Sidebar** ([layout.tsx](src/app/(admin)/layout.tsx)): substituídas 5 entradas (`Ecossistema`, `Healthchecks`, `Definições Google`, `RGPD`, `Histórico`) por **uma só "Sistema"** (icon `Cog`, href `/settings/google` como default) com nova prop `matchPaths: ["/settings", "/healthchecks", "/ecossistema"]` que activa o item em qualquer dessas URLs (active logic estendido para suportar matchPaths). **Lote D (Maps Places):** `npm install @googlemaps/js-api-loader` (API functional `setOptions`+`importLibrary` — a classe `Loader` está deprecated). Novo componente [src/components/address-autocomplete.tsx](src/components/address-autocomplete.tsx) — promessa partilhada (cache 1x por sessão) carrega `places` library; instancia `google.maps.places.Autocomplete` no input com `componentRestrictions: {country: "pt"}` e `types: ["geocode"]`; listener `place_changed` chama `onChange(formatted_address)`. **Fallback gracioso:** se `NEXT_PUBLIC_GOOGLE_MAPS_KEY` não estiver definida ou falhar a carregar, comporta-se como Input normal (warn no console + msg italic amarelo abaixo). Cleanup limpa listeners no unmount via `google.maps.event.clearInstanceListeners`. Usado no campo "Morada da recolha" do workbench da Preservação (`pickup_address`). **Type check + ESLint limpos.**

  **Maria precisa de fazer manualmente quando aceitar este lote:**
  1. Não há migração nem env vars obrigatórias
  2. **Para activar Google Maps autocomplete** (Lote D — opcional, mas é o que pediste):
     - Vai a [Google Cloud Console](https://console.cloud.google.com), cria projecto "FBR Admin" (ou usa o existente da Drive/Calendar)
     - APIs & Services → Library → activa **Maps JavaScript API** + **Places API**
     - APIs & Services → Credentials → "+ Create credentials" → API key
     - **Restringir a chave** (Application restrictions → HTTP referrers): `https://admin.floresabeirario.pt/*`, `https://*.vercel.app/*`, `http://localhost:3000/*`
     - **Limitar APIs** (API restrictions): só Maps JavaScript API + Places API
     - Adicionar ao `.env.local` e Vercel (Production + Preview): `NEXT_PUBLIC_GOOGLE_MAPS_KEY=AIza...`
     - Forçar redeploy no Vercel (não auto-redeploya ao mudar env vars)
  3. Push para Vercel (auto-deploy via main)
  4. Smoke test:
     - Sidebar: "Entregas e Recolhas" aparece indentada sob Preservação; sidebar tem só "Sistema" (não as 5 entradas antigas); clicar "FBR Admin" leva ao Dashboard
     - Workbench: clicar na bandeira do card "Comunicações" pede confirmação para trocar PT↔EN; "Inventário das flores" aparece na coluna esquerda como Card própria
     - Preservação → vista Calendário: testa Semana/Mês/Ano; em vista Ano clica num mini-mês — abre vista mensal desse mês; encomendas com `flower_delivery_method=recolha_evento` mostram ícone 🚐 antes do nome; clica num evento — abre popover com info + botão "Abrir encomenda"
     - Sistema: entra em qualquer das 5 sub-tabs — pill-tabs no topo permitem navegar entre Definições/RGPD/Histórico/Healthchecks/Ecossistema sem voltar ao sidebar
     - Morada da recolha: campo de morada no workbench (quando flower_delivery_method=recolha_evento) — se a chave Maps estiver configurada, autocomplete aparece; se não, comporta-se como input normal
     - Ana (viewer): entra em /ecossistema — pill-tabs mostram só "Ecossistema" (outras escondidas porque adminOnly)

- **Sessão 45 (parte 2) ✅ Dark mode — fix do workbench após review da Maria.** Maria viu o workbench de Preservação em dark e mostrou screenshot com problemas óbvios: (a) **fundo da página entre cards aparecia LIGHT cream** apesar do sidebar/header estarem dark — causa: `bg-[#F7F4F0]` hardcoded no wrapper do workbench ([preservacao/[id]/workbench-client.tsx:766](src/app/(admin)/preservacao/%5Bid%5D/workbench-client.tsx#L766), [vale-presente/[code]/workbench-client.tsx:263](src/app/(admin)/vale-presente/%5Bcode%5D/workbench-client.tsx#L263), `+ 3 loading.tsx`) — cor que escapou do meu script da parte 1; (b) **section headers com tint "muddy"** — o mapa local `ACCENTS` no workbench de Preservação tem `bgSoft: "bg-rose-50/50"` etc. — light X-50 a 50% sobre dark = washed out; (c) **STATUS_COLORS e PAYMENT_COLORS duplicados inline** no workbench de Preservação (não os do `_styles.ts` que já tinha tratado) — ficavam com cores light em badges. Fixes: (1) extensão do script `scripts/darkmode-tokens.ps1` com `bg-[#F7F4F0]` → `bg-cream-50`, `bg-[#F4EFE8]` → `bg-cream-100`, `border-[#E0D5C2]` → `border-cream-200`; (2) `ACCENTS` em [preservacao workbench:200](src/app/(admin)/preservacao/%5Bid%5D/workbench-client.tsx#L200) — cada accent ganha trio `dark:border-l-X-700 dark:text-X-400 dark:bg-X-950/30` (tint escuro tonal em vez de light washed out); (3) `ACCENT_STYLES` análogos em [vale-presente workbench:1039](src/app/(admin)/vale-presente/%5Bcode%5D/workbench-client.tsx#L1039), [nova-encomenda-sheet:458](src/app/(admin)/preservacao/nova-encomenda-sheet.tsx#L458), [novo-vale-sheet:518](src/app/(admin)/vale-presente/novo-vale-sheet.tsx#L518); (4) STATUS_COLORS + PAYMENT_COLORS locais do preservacao workbench ganharam o trio dark (mesmo padrão do `_styles.ts`). Type check limpo.

  **Maria precisa de fazer manualmente quando aceitar este lote:**
  1. Não há migração nem env vars
  2. Push para Vercel (auto-deploy)
  3. Revisitar o workbench de Preservação em dark — fundo agora deep-warm uniforme, section headers com tint tonal escuro (não mais "muddy"), badges de estado e pagamento com cores correctas

- **Sessão 45 ✅ Dark mode — palette refinada + cobertura sistemática.** Maria pediu para "melhorar o dark mode" — escolheu via `AskUserQuestion` "Ambos — completar páginas E refinar palette". Estado anterior: shell (sidebar/layout) tinha dark variants completas, mas ~300 ocorrências de hex hardcoded (`bg-[#FAF8F5]`, `border-[#E8E0D5]`, `text-[#3D2B1F]`, etc.) em 25-31 ficheiros sem variante `dark:` — workbenches, chat, status, listings, receitas, calendar-view, timeline tudo "vidro branco partido" em dark mode. Palette base era shadcn default (cinzas neutros) — não combinava com a marca FBR (castanhos quentes). **Estratégia escolhida:** introduzir **CSS variables semânticos** que fazem swap automático `.dark { ... }` em vez de adicionar `dark:` a cada classe individualmente. Centenas de edits viram um. **Tokens novos em [globals.css](src/app/globals.css):** `--cream-50/100/200` (surfaces e borders), `--cocoa-500/700/900` (escala de texto, inverte em dark), `--surface/--surface-hover` (card bg), `--btn-primary/--btn-primary-hover/--btn-primary-fg` (CTA chocolate que vira cream em dark). Light: cream-50=#FAF8F5, cocoa-900=#3D2B1F, btn-primary=#3D2B1F com fg white. Dark: cream-50=#15110D (deep warm black), cocoa-900=#E8D5B5 (cream), btn-primary=#E8D5B5 com fg #1B1611 (deep warm). Tokens expostos via `@theme inline { --color-cream-50: var(--cream-50); ... }` (sintaxe Tailwind v4). **Shadcn vars no `.dark` reescritos** com chroma quente (oklch hue ~60-80 em vez de 0 neutro): `--background: oklch(0.13 0.008 60)` (warm browny black) e foreground com tom cream. **Substituição em massa** via [scripts/darkmode-tokens.ps1](scripts/darkmode-tokens.ps1) — 3 passagens controladas que (a) convertem pares compostos primeiro (`bg-white dark:bg-[#141414]` → `bg-surface`), depois standalone (`bg-[#FAF8F5]` → `bg-cream-50`), depois cleanup de `dark:` redundantes (com guards de espaço para não partir variantes com opacity como `/60`); (b) convertem botões primários `bg-[#3D2B1F] hover:bg-[#2C1F15] text-white` → `bg-btn-primary text-btn-primary-fg hover:bg-btn-primary-hover`; (c) dividers e separators (`divide-[#F0EAE0]` → `divide-cream-100`). Total: **~70 ficheiros alterados** entre as passagens. **Edge cases tratados à mão:** install-prompt (gradient + botão primário); chat-client (border de reply); entregas-recolhas (ring focus); financas (pill activo); vale-presente confirm dialog; ecossistema (gradients de cards com dark variants explícitas). **Recharts:** [metricas-client.tsx](src/app/(admin)/metricas/metricas-client.tsx) ganha `useTheme()` (next-themes) e helpers `chartGrid/chartTooltipBg/chartTooltipBorder/chartTooltipText` derivados de `resolvedTheme === "dark"`. Todos os tooltips passam a usar `tooltipStyle` partilhado; `<CartesianGrid stroke={chartGrid} />`. `PieDist` é função separada — tem o seu próprio `useTheme()`. **Banners coloridos:** `STATUS_COLORS` e `PAYMENT_COLORS` em [preservacao/_styles.ts](src/app/(admin)/preservacao/_styles.ts) ganham variantes dark (`bg-X-950/40 text-X-200 border-X-900`) — propaga para tabela, calendar, timeline, cards (16 estados + 4 pagamentos). `STATUS_META` em [healthchecks-client.tsx](src/app/(admin)/healthchecks/healthchecks-client.tsx) idem (ok/warning/error/info). Dashboard alertas já tinham dark — confirmado. Type check (`tsc --noEmit`) limpo. ESLint dos ficheiros alterados também limpo (os 3 erros pré-existentes em theme-toggle / hard-delete-dialog / use-mobile não foram introduzidos nesta sessão — feedback memory já documenta).

  **Maria precisa de fazer manualmente quando aceitar este lote:**
  1. Não há migração nem env vars
  2. Push para Vercel (auto-deploy via main)
  3. Smoke test em DARK MODE (toggle pela ThemeToggle na sidebar):
     - **Listagem de Preservação**: badges de estado e pagamento — todos os 16 estados visíveis com contraste suficiente (cores -950/40 com texto -200)
     - **Workbench de encomenda**: hero, secções coloridas, campos de form, botões primários (devem aparecer com fundo cream em vez de branco)
     - **Workbench de Vale-Presente**: mesma verificação; o botão "Confirmar" no diálogo de pagamento deve ser cream
     - **Workbench de Parcerias**: mesma verificação
     - **Métricas**: gráficos com grid e tooltips legíveis em dark (linhas a cinza-warm, tooltips com fundo escuro)
     - **Status, Chat interno, Livro de Receitas, Ideias Futuras, Entregas e Recolhas, Healthchecks, Ecossistema**: todas as páginas com fundo warm-dark + cards `bg-surface` lifted
     - **Calendar e Timeline** da Preservação: dias passados/futuros e bolinhas de estado visíveis
  4. Reverter para light mode e confirmar que tudo continua igual (este é o teste crítico — os tokens são supostos NÃO mudar o look em light)
  5. Os banners coloridos em páginas individuais (`bg-rose-50` em sticky notes, alerts pontuais, etc.) ainda podem aparecer "demasiado brilhantes" em dark — uma sessão futura pode adicionar `dark:bg-X-950/40` a esses; nesta sessão só os badges de estado e payment (que estão em todas as listagens) e os healthchecks foram convertidos.

- **Sessão 44 ✅ Pesquisa global (Cmd+K).** Maria escolheu este item via `AskUserQuestion` (entre as 4 frentes pendentes da Fase 6: pesquisa global, Anthropic IA, Gmail, chat-media). Implementação sem migração — só código. **Server action** em [src/app/(admin)/search/actions.ts](src/app/(admin)/search/actions.ts): `globalSearchAction(query)` faz `Promise.all` de 5 queries (orders/vouchers/partners/ideas/recipes) com `.or()` ilike em todos os campos textuais relevantes; limita a 6 resultados por tipo; sanitiza a query removendo `,()*` (caracteres que partem o parser do `.or()` do PostgREST). `requireUser()` no topo (todos os 3 utilizadores podem usar, Ana inclusive). Cada hit é transformado em `SearchResult {kind,id,title,subtitle,meta,href}` com a URL certa do workbench (`/preservacao/<order_id>`, `/vale-presente/<code>`, `/parcerias/<id>`, `/livro-receitas/<id>`, `/ideias#<id>`). **Componente cliente** [src/components/global-search.tsx](src/components/global-search.tsx): listener `keydown` global para `Cmd/Ctrl+K` (toggle) + custom event `fbr-open-search` para abrir via botão. Debounce de 220ms no `useEffect` (setState dentro de `setTimeout` async — padrão permitido por [[feedback-react-set-state-in-effect]]); `setPending(true)` é feito no `onValueChange` síncrono para não violar a regra. **Race-condition guard**: token incremental `reqIdRef` invalida respostas obsoletas se o utilizador continuar a escrever. `Command shouldFilter={false}` — a filtragem é server-side via `ilike`, então o cmdk não filtra localmente. UI usa `CommandDialog` + `CommandGroup` agrupado por tipo (encomendas/vales/parceiros/receitas/ideias) com ícone próprio (Flower2/Gift/Handshake/BookOpen/Lightbulb), subtítulo (localização do evento / valor do vale / cidade do parceiro / nome científico), e badge meta (order_id / código / categoria / dificuldade / importância). Estados: "Escreve ≥2 caracteres", "A procurar…" (Loader2 a girar), "Nada encontrado para …". Rodapé com hints `↑↓ navegar · ↵ abrir · Esc fechar` e atalho `⌘+K` ou `Ctrl+K` consoante plataforma (detecção via `navigator.platform`). Helper exportado `openGlobalSearch()` dispatcha o custom event. **Integração no layout** ([src/app/(admin)/layout.tsx](src/app/(admin)/layout.tsx)): `<GlobalSearch />` montado uma vez no fim. **Botão pill na sidebar** entre o logo e o nav (estilo Linear/Slack — input fake com `Search` icon + texto "Procurar…" + kbd "Ctrl K"); em estado colapsado mostra só o ícone. **Botão na top-bar mobile** à direita do título (ícone Search). Type check (`tsc --noEmit`) e ESLint limpos.

  **Maria precisa de fazer manualmente quando aceitar este lote:**
  1. Não há migração nem env vars nesta sessão
  2. Push para Vercel (auto-deploy via main)
  3. Smoke test:
     - Pressiona `Ctrl+K` em qualquer página → palette abre
     - Escreve nome de cliente (≥2 caracteres) → resultados aparecem agrupados
     - Click num resultado → navega para o workbench correcto e fecha o dialog
     - Repete com código de vale (6 dígitos) e com nome de parceiro
     - Testa o botão "Procurar…" na sidebar e (em telemóvel) o ícone de lupa na top-bar
     - Ana (viewer) também consegue procurar (sem `requireAdmin()`, só `requireUser()`)

- **Sessão 43 ✅ Lacunas + ideias futuras promovidas a features.** Maria reviu o PROGRESS comigo, confirmou que estava preocupada por algumas coisas dela só viverem como "ideias futuras". Decidimos: fechar lacunas concretas detectadas na análise da sessão 42 (excepto backup automático — não é necessário); promover 3 ideias do PROGRESS.md a features reais ("Chat interno", "Livro de Receitas", "Comissão condicional"). Implementado num lote único (Chat interno em versão mínima — só texto — por escolha da Maria; media fica para sessão dedicada). **(A) Comissão condicional**: campos *Comissão (€)* + *Estado da comissão* agora condicionados a `partner_id` em ambos os workbenches ([preservacao/[id]/workbench-client.tsx](src/app/(admin)/preservacao/%5Bid%5D/workbench-client.tsx) e [vale-presente/[code]/workbench-client.tsx](src/app/(admin)/vale-presente/%5Bcode%5D/workbench-client.tsx)). **(B) Exports CSV vales + parceiros**: [src/lib/export-csv.ts](src/lib/export-csv.ts) ganha `exportVouchersToCsv` (32 colunas) e `exportPartnersToCsv` (16 colunas, exporta só os da categoria activa); refactor para `downloadCsv` helper partilhado; botões "Exportar" nas listagens de Vale-Presente e Parcerias (mesmo padrão visual de Preservação). Parceiros: filename inclui a categoria (`fbr-parceiros-floristas_2026-05-14_1530.csv`). **(C) Livro de Receitas** — nova aba, wiki por tipo de flor. Migração `028_recipes.sql`: tabela `recipes` com `flower_name`, `scientific_name`, `difficulty` (facil/media/dificil/experimental), `press_days_min/max`, `intro`, `steps JSONB` (`[{order, title, body, photo_url?}]`), `observations`, `photos JSONB` (`[{url, caption?}]`), `related_orders`. RLS dos 3 utilizadores (Ana inclusive — é wiki colaborativo). Audit log. Tipos em [src/types/recipe.ts](src/types/recipe.ts). Server actions em [livro-receitas/actions.ts](src/app/(admin)/livro-receitas/actions.ts). **Página `/livro-receitas`** ([livro-receitas-client.tsx](src/app/(admin)/livro-receitas/livro-receitas-client.tsx)): grid de cards com foto+nome+dificuldade+tempo prensa+nº passos+nº fotos; filtros por dificuldade + search; click no card → detalhe. **Página `/livro-receitas/[id]`** ([recipe-detail-client.tsx](src/app/(admin)/livro-receitas/%5Bid%5D/recipe-detail-client.tsx)): hero rosa com nome+científico+dificuldade+prensa; secções editáveis Apresentação, Passos numerados (add/remove/reordenar), Observações, Fotos (URL Drive ou directa). Auto-save no blur (mesmo padrão "store info from previous renders" para drafts). **(D) Chat interno mínimo (só texto + Realtime)** — versão definida pela Maria com `AskUserQuestion`. Migração `029_chat_messages.sql`: tabela `chat_messages` com `author_email`, `body`, `attachments JSONB` (preparado para foto/vídeo/áudio mas não usado nesta versão), `reply_to`, `read_by`. RLS dos 3 utilizadores: SELECT/INSERT/UPDATE separadas (INSERT força `author_email = JWT email`, UPDATE só permite ao autor). Publicada para `supabase_realtime` (com guard idempotente). Tipos em [src/types/chat.ts](src/types/chat.ts). Server actions em [chat/actions.ts](src/app/(admin)/chat/actions.ts). **Página `/chat`** ([chat-client.tsx](src/app/(admin)/chat/chat-client.tsx)): WhatsApp-style — header com info de membros, área de mensagens scrollável agrupada por dia, bubbles próprias castanhas (#3D2B1F) vs outras brancas, avatars apenas no início de bursts (stacking se autor igual e <5min entre mensagens), reply preview, hover para Responder/Apagar, Enter para enviar / Shift+Enter para quebra de linha. **Realtime subscription** via `supabase.channel("chat-messages").on("postgres_changes", ...)` — mensagens novas aparecem sem refresh. Auto-scroll usa padrão "store info from previous renders" (lastLengthRef + queueMicrotask) — sem `useEffect+setState` ([[feedback-react-set-state-in-effect]]). **(E) Finanças → Despesas**: migração `030_expenses.sql` — tabela `expenses` com `expense_date`, `supplier`, `category` (9 opções), `amount`, `vat_rate`, `payment_method` (6 opções), `has_invoice`, `invoice_url`, `notes`. RLS: admins escrevem, Ana lê. Tipos em [src/types/expense.ts](src/types/expense.ts). Server actions adicionadas a [financas/actions.ts](src/app/(admin)/financas/actions.ts). Nova tab `DespesasTab` em [financas-client.tsx](src/app/(admin)/financas/financas-client.tsx): KPIs (total, mês, filtrado), search + filtro por categoria, form de criação inline rosa, tabela com edição inline do método de pagamento via select, arquivar com confirm. **(F) Finanças → Faturação**: nova tab `FaturacaoTab` com KPIs (Receita do mês com delta % vs mês anterior, Receita do ano, Despesas do mês, Lucro do mês, Despesas do ano, Lucro do ano) — receita derivada proporcionalmente do `payment_status` das encomendas (100%=100%, 70%=70%, 30%=30%, por_pagar=0%) + vales `100_pago` que ainda não foram convertidos em preservação (evita dupla contagem com a encomenda). **Gráfico de barras manual** dos últimos 12 meses (receita verde vs despesas vermelhas) construído com `div`s — sem dependência adicional, suficiente para esta vista (a aba Métricas continua a ter o recharts mais sofisticado). Componente partilhado `KpiBox` com paleta (emerald/rose/sky/amber/slate/violet). [financas/page.tsx](src/app/(admin)/financas/page.tsx) agora carrega 5 fontes em paralelo (`Promise.all` de competitors, pricing, expenses, orders, vouchers) — só os campos estritamente necessários para evitar payload pesado. **(G) Healthchecks** — versão útil (escolhida pela Maria via `AskUserQuestion`). Página server-side em [healthchecks/page.tsx](src/app/(admin)/healthchecks/page.tsx) que (1) redirecciona não-admins, (2) corre verificações concretas e devolve `HealthCheck[]` ao client. Categorias: **Configuração** (presença de env vars `NEXT_PUBLIC_SUPABASE_*`, `GOOGLE_*`, `ANTHROPIC_API_KEY`, `RESEND_API_KEY` — só as 2 primeiras são obrigatórias, restantes opcionais), **Base de dados** (existência+contagem de 14 tabelas críticas via `select("*", { count: "exact", head: true })` — apanha `42P01` table-doesn't-exist e devolve hint), **Integridade dos dados** (encomendas com estado fora do enum, encomendas sem `client_name`, vales expirados não convertidos, pré-reservas sem contacto há ≥4 dias), **Integrações** (Google conectado? Pasta-mãe Drive criada? Calendar cacheado?). [healthchecks-client.tsx](src/app/(admin)/healthchecks/healthchecks-client.tsx) com resumo no topo (banner muda de cor consoante há erros/avisos), 4 pills clicáveis para filtrar por estado (ok/atenção/erro/info), agrupamento por categoria, cada linha com badge de estado + descrição + `hint` accionável (ex.: "💡 Vai a Definições Google e clica 'Conectar'"). Botão "Recarregar" no header. **(H) RGPD exportação PDF**: extensão minimal do `/rgpd-print` existente — [print-button.tsx](src/app/rgpd-print/print-button.tsx) agora detecta `?autoprint=1` e dispara `window.print()` automaticamente após 500ms (delay para fontes carregarem). [settings/rgpd/rgpd-client.tsx](src/app/(admin)/settings/rgpd/rgpd-client.tsx) ganha botão "Descarregar PDF" primário (Download icon) que abre `/rgpd-print?q=...&autoprint=1` em nova tab — o user só tem que escolher "Guardar como PDF" no diálogo de impressão. Mantidos os botões secundários "Exportar JSON" e "Abrir para visualizar". **(I) Sidebar**: 2 entradas novas — "Livro de Receitas" (`BookOpen`, rosa pelo tema) e "Chat interno" (`MessageCircle`). Posicionadas a seguir a "Entregas e Recolhas". **Lições:** (1) `useSyncExternalStore` é para subscrições externas — para tracking de estado anterior dentro do render usa-se o padrão "store info from previous renders" (ref + comparação directa no body do componente); (2) Supabase Realtime: `ALTER PUBLICATION supabase_realtime ADD TABLE x` falha se a publicação não existir (instalação manual) — embrulhar em `DO $$ ... EXCEPTION WHEN OTHERS THEN NULL`; (3) `auth.jwt()->>'email' = author_email` numa policy RLS é forma robusta de validar autoria mesmo sem `auth.uid()` (que pode ser NULL em sessões antigas).

  **Maria precisa de fazer manualmente quando aceitar este lote:**
  1. **Correr migrações 028, 029, 030** (por esta ordem) no Supabase SQL Editor:
     - `supabase/migrations/028_recipes.sql` — Livro de Receitas
     - `supabase/migrations/029_chat_messages.sql` — Chat interno (+ activa Realtime)
     - `supabase/migrations/030_expenses.sql` — Despesas
  2. Não há env vars novas
  3. Push para Vercel (auto-deploy via main)
  4. **No painel Supabase**, confirmar que `chat_messages` aparece em **Database → Replication → supabase_realtime** (a migração tenta activar, mas se falhar silenciosamente, fazer manual: Database → Replication → marcar `chat_messages`)
  5. Smoke test conforme lista no "Próximo passo CONCRETO" no fim do ficheiro

- **Sessão 42 ✅ Fase 6 parte 7 — Análise completa do projeto + grande batch de implementações.** Maria pediu (a) fix do botão errado no vale-presente que apontava para `status.floresabeirario.pt` em vez de `voucher.floresabeirario.pt/<código>` — corrigido em [vale-presente/[code]/workbench-client.tsx:451](src/app/(admin)/vale-presente/%5Bcode%5D/workbench-client.tsx#L451) (label "Página pública" → "Vale digital", URL corrigida); (b) análise completa do projeto vs spec do CLAUDE.md e ataque aos gaps. Identificados 5 gaps principais: integrações de comunicação (Gmail/WhatsApp/AI — deixados para sessões 43+), 4 abas placeholder, 3/4 sub-abas de Finanças placeholder, funcionalidades transversais (pesquisa global, backup automático, audit log UI). Esta sessão fechou os 3 maiores: **tabela de preços com cálculo automático**, **3 páginas placeholder**, e **audit log UI**, mais code-quality fixes. **(A) Migração `025_pricing.sql`**: tabela `pricing_items` com 3 categorias (`base_frame`, `background_supplement`, `extra`); unique `(category, key)` para mapear 1:1 aos enums de `orders.frame_size`/`frame_background`; RLS admin escreve + Ana lê; audit log; seed com valores plausíveis (30x40=300€, 40x50=400€, 50x70=500€; fundo cor=+20€, fotografia=+30€; mini-quadro=45€, ornamento=25€, pendente=15€ por unidade). Adiciona `orders.pricing_snapshot JSONB` para o snapshot congelado por encomenda. **Tipos** em [src/types/pricing.ts](src/types/pricing.ts). **Helper de cálculo** em [src/lib/pricing.ts](src/lib/pricing.ts): `computePricingSnapshot(order, pricing)` devolve `null` quando `frame_size` é "vocês a escolher" / "não sei" (orçamento fica manual nesses casos); caso contrário soma base + suplemento de fundo + extras×qty e devolve `{computed_at, total, lines:[…]}` com 1 linha por componente. **Server actions** em [financas/actions.ts](src/app/(admin)/financas/actions.ts) — apenas `updatePricingItemAction` (UPDATE; categorias e keys são fixas via migração, não se cria/apaga linhas pela UI). **`createOrderAction`** ([preservacao/actions.ts](src/app/(admin)/preservacao/actions.ts)) ganha bloco que, quando `budget` não foi passado manualmente, carrega `pricing_items`, chama `computePricingSnapshot`, e persiste `budget = total` + `pricing_snapshot` na linha. **Nova action `recomputeOrderBudgetAction`** para recalcular encomendas existentes com os preços actuais (útil em encomendas antigas). **UI Finanças → "Tabela de preços"** ([financas-client.tsx](src/app/(admin)/financas/financas-client.tsx)): nova tab `PrecosTab` agora default, com explicação do orçamento automático no topo (sky banner), tabela por categoria com inputs inline para preço e notas (auto-save no blur; "store info from previous renders" para sincronizar drafts com prop changes — [[feedback-react-set-state-in-effect]]). **Badge no workbench** ([preservacao/[id]/workbench-client.tsx](src/app/(admin)/preservacao/%5Bid%5D/workbench-client.tsx)): novo `BudgetSnapshotBadge` por baixo do input de orçamento. Sem snapshot: badge cinzento "Manual" + link "Calcular automaticamente" que chama o RPC. Com snapshot: badge verde com `Sparkles` — "Auto-calculado" se o budget actual bate certo com `snapshot.total`, ou "Auto · editado" se a Maria editou. Click → popover com o detalhe completo do snapshot (cada linha com label, qty e subtotal, total destacado em emerald, timestamp do snapshot, aviso amarelo se foi editado manualmente, botão "Recalcular com preços actuais"). Encomendas existentes ficam com snapshot NULL — só as criadas a partir desta migração usam o cálculo. **(B) Migração `026_ideas.sql`**: nova tabela `ideas` com importance (4 níveis), theme (8 temas), status (6 estados); RLS dos 3 utilizadores (Ana inclusive); seed com as 3 ideias que estavam no PROGRESS.md. Tipos em [src/types/idea.ts](src/types/idea.ts). Server actions em [ideias/actions.ts](src/app/(admin)/ideias/actions.ts) com `requireUser()`. **Página `/ideias`** ([ideias-client.tsx](src/app/(admin)/ideias/ideias-client.tsx)): toggle de agrupamento (Importância/Tema/Estado), filtro Mostrar/Esconder concluídas, form "Nova ideia" inline amarelo, cards com edição inline de título, importance, theme e status via select coloridos, autor + data, archive com confirm. **(C) Página `/entregas-recolhas`** ([entregas-recolhas-client.tsx](src/app/(admin)/entregas-recolhas/entregas-recolhas-client.tsx)): vista completa (não só 30 dias) com 4 KPIs filtráveis (Todas / Recolha no local / CTT flores / CTT quadro), search por cliente/local/ID, agrupamento por janela temporal (Atrasadas / Próximos 7 / 8-30 / Mais de 30), cada linha clicável para o workbench. Placeholder dedicado para "Calculadora de transporte" (Construction icon) à parte. **(D) Página `/ecossistema`** ([ecossistema/page.tsx](src/app/(admin)/ecossistema/page.tsx)): estática, sem BD. Fluxo principal (Entrada → Núcleo → Saída pública) com `floresabeirario.pt` → `admin.floresabeirario.pt` → `status.fbr` + `voucher.fbr`. Grid de integrações com badge "Activo / Pendente" por cada (Supabase, Vercel, Drive, Calendar, Gmail, WhatsApp, Anthropic, Turnstile). Nota final RGPD a apontar para `/settings/rgpd`. **(E) Sessão C — Code quality:** Fix do `useEffect(() => setPartner(initial), [initial])` em [parcerias/[id]/workbench-client.tsx:152](src/app/(admin)/parcerias/%5Bid%5D/workbench-client.tsx#L152) usando o padrão "store info from previous renders" (tracking `initial.updated_at`). Extracção dos componentes duplicados `StickyNoteButton` e `PartnerCombobox` para [src/components/sticky-note-button.tsx](src/components/sticky-note-button.tsx) e [src/components/partner-combobox.tsx](src/components/partner-combobox.tsx) — workbenches Preservação e Vale-Presente passam a importar do shared, removeu-se ~250 linhas duplicadas. StickyNoteButton ganhou props opcionais `title`, `label`, `placeholder` para diferenciar entre "Nota da encomenda" e "Nota do vale". **(F) Migração `027_audit_log_with_email.sql`** + UI: função RPC `audit_log_with_email(p_table, p_action, p_since, p_limit)` SECURITY DEFINER que faz LEFT JOIN com `auth.users` para devolver `changed_by_email`. Gate de admin dentro da função (segundo nível defensivo, complementa a policy RLS). Tipos em [src/types/audit.ts](src/types/audit.ts) com helper `diffValues()` que ignora `updated_at`/`public_status_updated_at`. **Página `/settings/audit`** ([audit-client.tsx](src/app/(admin)/settings/audit/audit-client.tsx)): redirecciona não-admins para `/`; filtros por tabela/acção/data (querystring), search interna nas 200 entradas carregadas, cada entrada colapsável mostrando diffs lado-a-lado (rose=antes, emerald=depois) ou JSON completo para INSERT/DELETE. Sidebar ganha entrada "Histórico" (`History` icon) por baixo de RGPD. Type check e ESLint limpos.

  **Maria precisa de fazer manualmente quando aceitar este lote:**
  1. **Correr migrações 025, 026 e 027** (por esta ordem) no Supabase SQL Editor:
     - `supabase/migrations/025_pricing.sql` — tabela `pricing_items` + seed + `orders.pricing_snapshot`
     - `supabase/migrations/026_ideas.sql` — tabela `ideas` + seed das 3 ideias do PROGRESS
     - `supabase/migrations/027_audit_log_with_email.sql` — RPC `audit_log_with_email`
  2. Não há env vars novas
  3. Push para Vercel (auto-deploy via main)
  4. Smoke test:
     - **Finanças → Tabela de preços**: ver as 3 categorias, editar um preço (ex.: 50x70 = 520€), confirmar que salva no blur, confirmar que o seed automático aparece bem
     - **Criar uma encomenda nova** com tamanho 40x50 e fundo cor: confirmar que o orçamento aparece já preenchido (400+20=420€) com badge verde "Auto-calculado" no workbench
     - **Workbench de encomenda existente**: confirmar que aparece badge "Manual" e link "Calcular automaticamente" (encomendas pré-sessão-42 ficam com snapshot NULL por design)
     - **Recalcular**: clicar "Recalcular com preços actuais" no popover do badge — confirmar que substitui o budget e o snapshot
     - **Ideias Futuras**: ver as 3 ideias do seed; criar uma nova; mudar importance/theme/status inline; arquivar
     - **Entregas e Recolhas**: confirmar que mostra encomendas com `flower_delivery_method=recolha_evento` ou `=ctt`; testar os 4 filtros e a search
     - **Ecossistema**: confirmar que abre com o fluxo principal e os cards de integrações
     - **Sidebar → Histórico (só admin)**: confirmar que carrega últimas 200 entradas; testar filtros por tabela/acção/data; clicar numa entrada para expandir o diff
     - **Vale-Presente workbench**: confirmar que o botão antes "Página pública" agora diz "Vale digital" e abre `voucher.floresabeirario.pt/<código>`
     - **Ana (viewer)**: confirmar que `/settings/audit` redirecciona para `/`; em Finanças → Tabela de preços os inputs estão disabled mas vê os valores

- **Sessão 41 ✅ Fase 6 parte 6 — RGPD avançado: exportação de dados pessoais + retenção de 10 anos.** Implementação completa do Art. 15 RGPD (direito de acesso) + política de retenção fiscal portuguesa. **Migração `024_rgpd_anonymize.sql`**: adiciona `anonymized_at TIMESTAMPTZ` em `orders` e `vouchers`; index parcial `orders_retention_idx` em `frame_delivery_date` filtrado por `deleted_at IS NULL AND anonymized_at IS NULL AND status='quadro_recebido'` (suporta listagem de retenção sem table scan); funções **`anonymize_order(uuid)`** e **`anonymize_voucher(uuid)`** SECURITY DEFINER que substituem TODA a PII por NULL ou `'[anonimizado]'` (nome, email, telefone, NIF, anexos, morada, mensagens públicas, sticky notes, consent_ip) preservando campos agregáveis (status, payment_status, budget, partner_id, how_found_fbr, frame_size, etc — necessários para métricas fiscais agregadas). Idempotente: `AND anonymized_at IS NULL` impede dupla anonimização. GRANT EXECUTE só para `authenticated` (a server action também faz `requireAdmin`). **Tipos** em [src/types/database.ts](src/types/database.ts) e [src/types/voucher.ts](src/types/voucher.ts) ganham `anonymized_at: string | null`. **Lib `src/lib/rgpd.ts`** (server-only): `searchClientData(query)` faz `Promise.all` em `orders` (campos `email`, `phone`) e `vouchers` (campos `sender_email`, `sender_phone`, `recipient_contact`) com ilike case-insensitive e exclui `anonymized_at NOT NULL`; `retentionDeadline()` calcula `frame_delivery_date + 10 anos`; `retentionStatus()` classifica em `expired`/`due_soon`/`future` (janela de alerta `RETENTION_ALERT_MONTHS=6`); `listOrdersDueForRetention()` devolve só `expired`+`due_soon`. **Server actions** ([settings/rgpd/actions.ts](src/app/(admin)/settings/rgpd/actions.ts)): `anonymizeOrderAction`/`anonymizeVoucherAction` chamam o RPC; `hardDeleteOrderForRgpdAction`/`hardDeleteVoucherForRgpdAction` para o direito ao esquecimento (Art. 17) — inserem audit_log com `rgpd_hard_delete_justification` antes do DELETE (porque o trigger automático regista `old_values` mas perde a razão pela qual a Maria escolheu apagar definitivamente). **Página `/settings/rgpd`** ([page.tsx](src/app/(admin)/settings/rgpd/page.tsx) + [rgpd-client.tsx](src/app/(admin)/settings/rgpd/rgpd-client.tsx)): redirecciona não-admins para `/`; pill-tabs com 2 secções — **Exportar** (search por email/telemóvel → mostra contagem de encomendas+vales + links rápidos para cada um + botões "Exportar JSON" e "Abrir versão para imprimir / PDF") e **Retenção 10 anos** (badge contando linhas em alerta no header da tab; card de política a explicar anonimizar vs apagar; agrupado em "Prazo já expirado" rose e "A expirar nos próximos meses" amber). Cada linha de retenção tem botões "Anonimizar" (com diálogo de confirmação) e "Apagar" (que abre o `HardDeleteDialog` existente reutilizado). **API JSON** [/api/rgpd/export/json](src/app/api/rgpd/export/json/route.ts): valida admin via `getCurrentRole`, recebe `q=` no querystring, devolve `{rgpd_export: {generated_at, legal_basis, query, summary}, orders: [...], vouchers: [...]}` em JSON pretty-printed com `Content-Disposition: attachment` e nome de ficheiro sanitizado (`fbr-rgpd-<query>-YYYY-MM-DD.json`). **Página de impressão** [/rgpd-print](src/app/rgpd-print/page.tsx): deliberadamente fora do route group `(admin)` para não herdar o sidebar/topbar — é uma página standalone de impressão. CSS inline com `@page { margin: 1.5cm }`, `.no-print` para esconder o botão "Imprimir" durante print, `page-break-inside: avoid` em cada `.record`. Tipografia da brand (Google Sans + TanMemories para o H1). `OrderBlock` e `VoucherBlock` listam TODOS os campos relevantes com labels traduzidos (STATUS_LABELS, EVENT_TYPE_LABELS, etc) em `<dl>` com grid 200px/1fr. Pequeno componente client `print-button.tsx` chama `window.print()`. Mantém-se admin-protected via `getCurrentRole()` no servidor + proxy global. **Sidebar** ([layout.tsx](src/app/(admin)/layout.tsx)): nova entrada "RGPD" (ícone `Shield` lucide) por baixo de "Definições Google", filtrada pelo mesmo padrão `href.startsWith("/settings")` que já só mostra a admins. Type check (`tsc --noEmit`) e ESLint dos ficheiros novos limpos.

  **Maria precisa de fazer manualmente quando aceitar este lote:**
  1. **Correr migração 024** (`supabase/migrations/024_rgpd_anonymize.sql`) no Supabase SQL Editor
  2. Não há env vars novas
  3. Push para Vercel (auto-deploy via main)
  4. Smoke test:
     - Sidebar → RGPD → tab "Exportar" → pesquisar com um email real → confirmar contagem + clicar "Exportar JSON" (baixa ficheiro) + clicar "Abrir versão para imprimir / PDF" (abre nova tab, conteúdo formatado, botão "Imprimir" funciona)
     - Tab "Retenção 10 anos" → confirmar que mostra placeholder (provavelmente lista vazia porque ainda não há encomendas com `frame_delivery_date` há ≥9.5 anos). Para testar a UI, podes temporariamente mudar a `frame_delivery_date` de uma encomenda concluída para 2016-01-01 e ver as alertas.
     - Ana (viewer) → confirmar que `/settings/rgpd` redirecciona para `/` (sem permissão)

- **Sessão 40 ✅ Fase 6 parte 5 — Mobile-friendly + fix do ícone PWA.** Maria instalou a PWA no iPhone na Sessão 39 mas (1) o ícone do ecrã principal apareceu vazio e (2) a plataforma estava completamente inutilizável no telemóvel. Causa do ícone: o `public/favicon/site.webmanifest` antigo (estático) tinha icon paths a apontar para `/android-chrome-192x192.png` em vez de `/favicon/android-chrome-192x192.png` — os ficheiros existem mas em subpasta. Como ela tinha instalado ANTES do deploy do meu manifest novo, o telemóvel cacheou a versão broken. **Fix:** (a) removi a declaração `purpose: "maskable"` do manifest porque o ícone de 512×512 tem as flores até às bordas e a máscara Android cropa-as; (b) próximo Maria desinstala+reinstala. Mobile work — abordagem deliberadamente conservadora ("desktop é o principal, mobile é conveniência ocasional"): **(admin) layout** ([src/app/(admin)/layout.tsx](src/app/(admin)/layout.tsx)) reescrito para responsivo. Hook `useIsDesktop()` (useSyncExternalStore com `matchMedia('(min-width: 1024px)')`) substitui a única lógica desktop antiga. Em desktop (≥lg) o sidebar continua igual — colapsa entre 64px/224px, click no fundo toggle, etc. Em mobile o sidebar vira drawer: fixed inset-y-0 left-0 z-50, slide via `translate-x`, fecha automaticamente ao mudar de rota (pattern "store info from previous renders" para respeitar [[feedback-react-set-state-in-effect]]); X dentro do header do drawer + backdrop preto/40 clicável + botão de fechar; novo top-bar `lg:hidden` no topo com hambúrguer + brand. Tap targets do nav passam de `py-2` (desktop) para `py-2.5` (mobile). **Page-level padding**: todas as páginas placeholder (ecossistema, ideias, healthchecks, entregas-recolhas) passam de `p-8` para `p-4 sm:p-8`; páginas principais (dashboard, financas, metricas, status, mensagens-default) passam de `p-6 lg:p-8` para `p-3 sm:p-6 lg:p-8` + `space-y-4 sm:space-y-6`. **Listings** (preservacao/vale-presente/parcerias): headers passam de `flex items-center justify-between gap-4 px-6 py-4` para `flex flex-wrap items-center justify-between gap-3 px-3 sm:px-6 py-3 sm:py-4` (permite wrap em mobile, padding mais apertado); título com `text-lg sm:text-xl`; o grupo de search+botões passa a `w-full sm:w-auto` para ocupar uma linha completa abaixo do título; input de search com `flex-1 sm:flex-none` para esticar até ao fim em mobile; alturas dos inputs e botões `h-9 sm:h-8` (tap targets maiores no telemóvel). **Tabelas**: todas com `overflow-x-auto` já existiam mas faltava `min-w-[Xpx]` na tabela própria — sem isto, `table-fixed w-full` com colunas em % aceitava encolher para 375px e ficava ilegível. Aplicado `min-w-[920px]` (preservação), `min-w-[820px]` (vale-presente), `min-w-[780px]` (parcerias, 2 sítios), `min-w-[860px]` (status). **Workbenches**: top-level já era `grid-cols-1 lg:grid-cols-N` — não precisou de mudança no body. Headers passam a `flex-wrap` (especialmente importante na Preservação que tinha 8+ elementos numa linha) com `gap-x-3 gap-y-2`; o StatusSelect de largura fixa `w-56 shrink-0` passa a `w-full sm:w-56 order-last sm:order-none` (vai para o fim em mobile, ocupa linha completa); padding do body wrapper `p-6` → `p-3 sm:p-6`. **Dashboard**: form de nova tarefa com `grid-cols-3` (cramped em mobile) → `grid-cols-1 sm:grid-cols-3`. **Lições:** (1) Tailwind `lg:` em Next 15+ + React 19 funciona perfeitamente sem JS; só usei `useIsDesktop()` no layout porque preciso de diferenciar comportamento JS (drawer state vs collapsed state); (2) `useSyncExternalStore` com `matchMedia` é o caminho limpo para detectar breakpoints em JS sem violar SSR ou `react-hooks/set-state-in-effect`; (3) `min-w-[...]` em tabelas `table-fixed` é obrigatório para a coluna `colgroup` em % funcionar com horizontal scroll. Type check (`tsc --noEmit`) limpo; ESLint dos ficheiros novos limpo. (Há um error pre-existente na linha 152 de `parcerias/[id]/workbench-client.tsx` — `useEffect` com setState — não introduzido nesta sessão, fica para uma futura sessão de cleanup.)

  **Maria precisa de fazer manualmente quando aceitar este lote:**
  1. Não há migração nem env vars
  2. Push para Vercel (auto-deploy)
  3. **No telemóvel:** desinstalar o atalho actual da app (toca-se prolongadamente no ícone → Remover), depois voltar a abrir `admin.floresabeirario.pt` no Chrome/Safari → "Adicionar ao ecrã principal". Desta vez o ícone das 3 flores aparece.
  4. Smoke test mobile:
     - Top bar com hambúrguer aparece em < 1024px
     - Hambúrguer abre drawer; X ou backdrop fecha
     - Mudança de rota fecha o drawer automaticamente
     - Páginas Preservação/Vale/Parcerias: headers fazem wrap em vez de overflow; search ocupa a linha
     - Tabelas: scroll horizontal sem squish (não ficam ilegíveis)
     - Workbench abre em 1 coluna, sem overflow no header

- **Sessão 39 ✅ Fase 6 parte 4 — PWA instalável no telemóvel.** Maria decidiu deixar Gmail/WhatsApp/AI para o fim (memória [[feedback-fase6-ordem-integracoes]]); abordamos primeiro PWA + RGPD avançado. Sessão 39 fecha o PWA. **Manifest** ([src/app/manifest.ts](src/app/manifest.ts)): novo ficheiro tipado (`MetadataRoute.Manifest`) que substitui o static `public/favicon/site.webmanifest` (mantido em disco mas já não referenciado — Next 16 injecta auto `<link rel="manifest">` do file convention). Campos: name "FBR Admin — Flores à Beira Rio", short_name "FBR Admin", description, start_url+scope `/`, display `standalone`, orientation `portrait`, background+theme `#FAF8F5`, lang `pt-PT`, categories `business`+`productivity`, 6 entradas de ícone (16/32 favicon, 192/512 android-chrome com purpose `any`, 512 maskable, 180 apple-touch). **Layout** ([src/app/layout.tsx](src/app/layout.tsx)): removido `manifest:` (auto-injectado pelo file convention); adicionado `applicationName`, `appleWebApp.capable:true`+title+statusBarStyle, `formatDetection.telephone:false` (impede iOS de transformar números em links automaticamente — relevante porque temos muitos telemóveis nas listagens); `viewport` ganhou `width: device-width`, `initialScale: 1`, `viewportFit: cover` (respeita notch em iPhone). **Service worker** ([public/sw.js](public/sw.js)): estratégia deliberadamente conservadora — admin lida com dados sensíveis e mostrar stale data confundiria a Maria. Cache-first APENAS para assets imutáveis (`/_next/static/*`, `/favicon/*`, `/userphotos/*`, `/fonts/*` e qualquer `.css/.woff2/.png/etc`); HTML, `/api/*`, `/auth/*` e Supabase passam SEMPRE pela network (não há fallback offline disfarçado). `CACHE_VERSION` bumpável para invalidar tudo. `install` faz `skipWaiting`; `activate` apaga caches antigos + `clients.claim`. **Registo do SW** ([src/components/pwa-register.tsx](src/components/pwa-register.tsx)): client component, `useEffect` regista em `/sw.js` com `scope:/` + `updateViaCache:'none'`, só em produção (skip dev/preview). Faz `registration.update()` proactivo para apanhar versões novas. **InstallPrompt** ([src/components/install-prompt.tsx](src/components/install-prompt.tsx)): pill flutuante bottom-right (mobile: bottom-full, desktop: right corner) com 2 modos — (a) Android/Chromium captura `beforeinstallprompt` e mostra botão "Instalar" (cor brand `#3D2B1F`); (b) iOS Safari (detecção via UA + `maxTouchPoints>1` para iPads modernos que reportam como Macintosh) mostra instruções "Toca em [share] e depois [+] Ecrã Principal" com ícones lucide. Hide automático quando `display-mode: standalone` ou `navigator.standalone === true`. Dismissível via localStorage `fbr-pwa-install-dismissed`. Usa `useSyncExternalStore` para `isIOS`, `isStandalone` e o estado inicial de `dismissed` (evita setState-em-useEffect — [[feedback-react-set-state-in-effect]] — e SSR mismatches). `appinstalled` listener limpa o `deferredPrompt`. **next.config.ts**: headers globais `X-Content-Type-Options: nosniff` + `Referrer-Policy: strict-origin-when-cross-origin`; headers específicos para `/sw.js` (`Content-Type: application/javascript`, `Cache-Control: no-cache,no-store,must-revalidate`, `Service-Worker-Allowed: /`). NÃO adicionei `X-Frame-Options: DENY` por causa do iframe potencial das integrações Google (Drive viewer, status público) — pode ser endurecido depois. **Lições:** (1) Next 16 file convention `app/manifest.ts` serve automaticamente em `/manifest.webmanifest` + injecta `<link>` no head — não precisa de `metadata.manifest` explícito; (2) `useSyncExternalStore` com `subscribe = () => () => {}` é o padrão limpo para snapshots one-shot de browser globals sem violar `react-hooks/set-state-in-effect`; (3) o iPad moderno reporta UA como `Macintosh` — para detectar precisa-se de `navigator.maxTouchPoints > 1`. Type check (`tsc --noEmit`) e ESLint dos ficheiros novos limpos.

  **Maria precisa de fazer manualmente quando aceitar este lote:**
  1. Não há migração nem env vars nesta sessão
  2. Push para Vercel (auto-deploy via main); o SW só fica activo em produção (skip em dev/preview por design)
  3. Smoke test mobile:
     - **iPhone Safari**: abrir `admin.floresabeirario.pt`, esperar pill com instruções "Toca em ⎋ e depois ➕ Ecrã Principal" → seguir → abrir do ecrã inicial → confirmar que abre em standalone (sem barra do Safari)
     - **Android Chrome**: abrir o mesmo URL, esperar pill com botão "Instalar" → tocar → confirmar instalação → abrir do drawer → standalone
     - **Desktop Chrome**: pode aparecer ícone de instalar na barra de endereços
  4. Confirmar no DevTools (Application → Manifest) que tudo está correcto e (Application → Service Workers) que `/sw.js` está active

- **Sessão 38 ✅ Fase 6 parte 3 — Google Calendar (criação automática de eventos ao 1º pagamento).** Reaproveita a foundation OAuth da sessão 37. Vales **não** geram eventos (decisão da Maria — só encomendas). **Migração `023_order_calendar_event.sql`**: adiciona `orders.calendar_event_id TEXT` (nullable; persistido sempre que um evento é criado para podermos depois actualizar/apagar). A coluna `calendar_id` em `google_integration` já existia da migração 022. **Helper `src/lib/google/calendar.ts`**: `ensureCalendar()` é idempotente — verifica cache em `google_integration.calendar_id` (com `calendars.get` para confirmar que ainda existe), depois faz fallback para `calendarList.list` (caso a Maria já tenha criado o calendário manualmente), e só por último faz `calendars.insert` com summary "Preservação de Flores", timeZone "Europe/Lisbon". `upsertOrderEvent(order)` constrói eventos all-day (start/end em formato `YYYY-MM-DD`; end exclusivo +1 dia) com summary `[Tipo de evento] — [client_name ou couple_names se casamento]` (e.g. "Casamento — Maria & João" ou "Funeral — Ana Costa"), description com `Encomenda #order_id` + URL do workbench, location de `event_location`, transparency=`transparent` (não bloqueia o calendário). Se já há `calendar_event_id` faz UPDATE; se o UPDATE devolver 404/410 (evento apagado a mão) cai para INSERT. `deleteOrderEvent(eventId)` é silencioso em 404/410. **Trigger `src/lib/google/order-calendar-trigger.ts`**: 3 helpers exhaustive — `isFirstOrderPaymentTransition` (igual ao de Drive), `statusBecomesCancelled` (`prev !== 'cancelado' && next === 'cancelado'`), `calendarFieldsChanged` (verifica se `event_date`, `client_name`, `event_type`, `couple_names` ou `event_location` mudaram). `upsertOrderCalendarEvent` e `deleteOrderCalendarEvent` chamam o helper acima, persistem `calendar_event_id` em `orders`, e em caso de erro logam silenciosamente em vez de rebentar (mesmo padrão do Drive). **Integração no `updateOrderAction`** ([preservacao/actions.ts](src/app/(admin)/preservacao/actions.ts)): substituí o fetch único do `prev` (que só trazia `payment_status, drive_folder_id`) por um fetch alargado com `status, calendar_event_id, event_date, client_name, event_type, couple_names, event_location`. Lógica em 3 cenários por ordem de prioridade: (1) `cancelado` + tinha evento → DELETE; (2) 1º pagamento + ainda sem evento → CREATE; (3) já tinha evento + algum campo visível mudou → UPDATE. Drive e Calendar coexistem (o 1º pagamento dispara ambos). **Server actions manuais** `createOrderCalendarEventAction(id)` e `deleteOrderCalendarEventAction(id)` — usadas pelo workbench (encomendas antigas ou retry após erro). **Componente `CalendarEventShortcut`** no workbench (ao lado do botão Pasta Drive + Status público): sem `event_date` mostra pill cinzenta desactivada; com `event_date` mas sem evento mostra pill violet tracejada "Criar no Calendar"; com evento mostra pill violet sólida "No Calendar" + lápis lateral que abre popover com "Re-sincronizar evento" (violet) + "Apagar evento" (rose). Toast de sucesso/erro via sonner + `router.refresh()` para reflectir o `calendar_event_id` na BD. **Página `/settings/google`** ganha card "Calendário 'Preservação de Flores'" com check verde se `calendar_id` está cacheado + link `https://calendar.google.com/calendar/u/0/r?cid=<id>` (abrir no GCal web), e botão "Verificar / criar calendário agora" (chama `ensureCalendarAction`). **Lições:** (1) Calendar all-day events: o `end.date` é EXCLUSIVO, por isso para um evento de 1 dia somamos +1 ao `start.date` (em formato `YYYY-MM-DD`); (2) googleapis lança erros com `code` ou `status` consoante o caminho — apanho ambos no `try/catch` para o fallback 404→INSERT funcionar; (3) `transparency: "transparent"` faz com que o evento não conte como "ocupado" no calendário principal — não interfere com a disponibilidade da Maria. Type check (`tsc --noEmit`) e ESLint dos ficheiros novos limpos.

  **Maria precisa de fazer manualmente quando aceitar este lote:**
  1. **Correr migração 023** (`supabase/migrations/023_order_calendar_event.sql`) no Supabase SQL Editor
  2. Não precisa de mexer em env vars nem em OAuth — o scope `calendar` já estava no consent da sessão 37 e o refresh_token já está guardado
  3. Login admin → sidebar "Definições Google" → secção "Calendário 'Preservação de Flores'" → "Verificar / criar calendário agora" (opcional — também é criado automaticamente ao 1º evento)
  4. Smoke test: numa encomenda paga já existente, clica em "Criar no Calendar" no workbench → confirma que o evento aparece no Google Calendar com a data correcta; depois muda a data do evento → confirma que o evento se actualiza; muda para `cancelado` → confirma que o evento desaparece

- **Sessão 37 ✅ Fase 6 parte 2 — Foundation OAuth Google + auto-criação de pastas na Drive ao 1º pagamento.** A Maria pediu Drive (pasta cliente) e Calendar como prioridades; esta sessão fechou Drive completo + foundation OAuth partilhado com Calendar (que fica para sessão 38). **Migração `022_google_integration.sql`**: tabela singleton `google_integration` (com unique index `((true))` para garantir 1 só registo) com `refresh_token`, `google_email`, `scopes`, IDs cache de Drive folders (`drive_root_folder_id`, `drive_orders_folder_id`, `drive_vouchers_folder_id`), `calendar_id` (placeholder para sessão 38), `connected_at` + `connected_by_email`. RLS só admins (António, MJ) — Ana **não** vê (contém refresh_token sensível). Adiciona `drive_folder_id` (string ID) em `orders` e `vouchers` para além do `drive_folder_url` que já existia. GRANT `authenticated`. **Pacote `googleapis@^144.0.0`** adicionado ao package.json. **Helper OAuth** ([src/lib/google/oauth.ts](src/lib/google/oauth.ts)): `getOAuthClient()` cria `google.auth.OAuth2` com client_id/secret das env vars (lança erro descritivo se faltarem); `buildConsentUrl(state)` gera URL de consent com `access_type=offline + prompt=consent` (para forçar refresh_token); `loadIntegration()` / `saveIntegrationAfterConsent()` / `disconnectIntegration()` lidam com a tabela singleton; `getAuthenticatedClient()` devolve OAuth2Client já com `refresh_token` setado — googleapis trata da renovação de access_token automaticamente. Scopes: Drive + Gmail read-only + Calendar + openid/email/profile. **Endpoints OAuth** ([api/google/connect/route.ts](src/app/api/google/connect/route.ts) e [api/google/callback/route.ts](src/app/api/google/callback/route.ts)): `/connect` gera state random 16 bytes hex, guarda em cookie httpOnly samesite=lax 10min, redirecciona para Google. `/callback` valida state vs cookie, troca `code` por `tokens`, valida que `userinfo.email === info@floresabeirario.pt` (rejeita outras contas), persiste refresh_token e redirecciona para `/settings/google?ok=1`. Tratamento de erros: `state_mismatch`, `no_code`, `no_refresh_token` (se Google não devolve — happens se utilizador já consentiu antes), `wrong_account`, `exchange_failed`. **Helper Drive** ([src/lib/google/drive.ts](src/lib/google/drive.ts)): constantes `DRIVE_ROOT_NAME` ("FBR — Encomendas"), `DRIVE_ORDERS_NAME` ("Preservação de Flores"), `DRIVE_VOUCHERS_NAME` ("Vale-Presente"), `ORDER_SUBFOLDERS` (8: Comprovativos, Faturas, 1. Receção das flores, 2. Preservação, 3. Reconstrução, 4. Composição e colagem, 5. Emolduramento, 6. Resultado final), `VOUCHER_SUBFOLDERS` (2: Comprovativos, Faturas). `findFolderByName` faz `drive.files.list` com `q=mimeType='folder' and name='X' and parents='Y' and trashed=false`. `ensureFolder` é idempotente — devolve ID existente ou cria. `ensureRootFolders()` cria/cacheia as 3 pastas-mãe e persiste IDs em `google_integration`. `ensureOrderFolder({customerName, eventDate})` cria pasta "[Nome] | dd/MM/yyyy" dentro de Preservação + 8 subpastas; `ensureVoucherFolder({senderName, createdAt})` análogo com data de criação e 2 subpastas. **Sanitização**: nomes do cliente passam por `sanitize()` que remove `\\/<>:"|?*` (caracteres ilegais em paths). **Trigger** ([src/lib/google/order-drive-trigger.ts](src/lib/google/order-drive-trigger.ts)): `isFirstOrderPayment(prev, next)` retorna true quando `prev === '100_por_pagar' && next !== '100_por_pagar'`; análogo para vouchers (só transição `100_por_pagar → 100_pago`). `createOrderDriveFolderIfNeeded` / `createVoucherDriveFolderIfNeeded`: skip se já tem `drive_folder_id`, skip se Google não conectado (retorna null silenciosamente, não rebenta); caso contrário cria pasta e persiste `drive_folder_id` + `drive_folder_url` na linha. Falhas → `console.error` + retorna null, não rebenta o update. **Integração no `updateOrderAction`** ([src/app/(admin)/preservacao/actions.ts](src/app/(admin)/preservacao/actions.ts)): antes do UPDATE, se `updates.payment_status` está definido, fetch do `payment_status` actual + `drive_folder_id`; se for 1º pagamento e ainda não tem pasta, marca `triggerDriveCreation = true`; após o UPDATE chama o helper que cria (~3-5s para 11 chamadas API, 1 cliente + 8 sub, cache hits para mãe). Mesma lógica em `updateVoucherAction`. **Server actions manuais** `createOrderDriveFolderAction(id)` e `createVoucherDriveFolderAction(id)` para criar/garantir manualmente (botões no workbench e na settings). **Página `/settings/google`** ([page.tsx](src/app/(admin)/settings/google/page.tsx) + [settings-client.tsx](src/app/(admin)/settings/google/settings-client.tsx)): redirecciona não-admins para `/`; mostra estado conectado/não-conectado com email/scopes/timestamps; banner ok/erro com mapping de códigos de erro para mensagens PT; botão "Conectar Google" (link para `/api/google/connect`); botão "Desconectar" com confirm; lista das 3 pastas-mãe com check verde se cache populado (link "abrir"); botão "Verificar / criar pastas-mãe agora"; secção colapsável "Antes de conectar" com guia passo-a-passo para criar OAuth client no Google Cloud Console e env vars necessárias (GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, GOOGLE_REDIRECT_URI). **Workbench da Preservação** ([workbench-client.tsx](src/app/(admin)/preservacao/[id]/workbench-client.tsx)): `DriveUrlEditor` repaginado — agora tem botão verde grande "Criar automaticamente na Drive" (chama `createOrderDriveFolderAction`) e abaixo um separador "ou cola um URL manualmente" para casos em que a Maria já tem uma pasta antiga. Toast de sucesso/erro via sonner (já configurado globalmente no root layout). **Sidebar** ([layout.tsx](src/app/(admin)/layout.tsx)): nova entrada "Definições Google" (ícone Settings) no fim do nav, filtrada para só aparecer a admins (Ana não vê). **Lições:** (1) o nome da coluna na tabela `orders` é `client_name` e não `customer_name` (apanhei isto no tsc); (2) tipos `Voucher.created_at` é `string` (não-null) mas no Supabase select volta `string | null` se a row foi recém-criada — o helper aceita `string | null` para ser resiliente; (3) `Order` é grande, `Pick` é seguro para o trigger não acoplar a tudo. Type check (`tsc --noEmit`) e ESLint dos ficheiros novos limpos.

  **Maria precisa de fazer manualmente quando aceitar este lote:**
  1. **Correr migração 022** (`supabase/migrations/022_google_integration.sql`) no Supabase SQL Editor
  2. **Criar OAuth client no Google Cloud Console** (passos detalhados no card "Antes de conectar" em `/settings/google`):
     - APIs: Drive + Gmail + Calendar
     - OAuth Consent Screen: External / Testing / adicionar info@floresabeirario.pt como test user
     - Credenciais OAuth 2.0 (Web) com redirect URIs:
       - `https://admin.floresabeirario.pt/api/google/callback`
       - `http://localhost:3000/api/google/callback`
  3. **Env vars no Vercel** (e `.env.local` para dev):
     - `GOOGLE_CLIENT_ID`
     - `GOOGLE_CLIENT_SECRET`
     - `GOOGLE_REDIRECT_URI` (opcional; fallback usa `NEXT_PUBLIC_SITE_URL`)
  4. **Redeploy** (Vercel não auto-redeploya ao mudar env vars)
  5. Login admin → sidebar "Definições Google" → "Conectar Google" → consent
  6. Smoke test: muda payment_status de uma encomenda de prova de "100% por pagar" para "30% pago" — a pasta deve aparecer em `https://drive.google.com/drive/folders/` com as 8 subpastas

- **Sessão 36 ✅ Fix — fase pública nova "A finalizar o quadro" passa a aparecer no site `status.floresabeirario.pt`.** Maria notou que o estado interno `a_finalizar_quadro` (adicionado na sessão 28) não estava a aparecer como passo próprio na timeline pública — partilhava a fase 6 ("A aguardar aprovação") porque era a decisão temporária da sessão 28. Agora ganhou uma fase pública dedicada. **Mudanças:** (1) [src/lib/public-status.ts](src/lib/public-status.ts) — `PublicPhase` extendida para `0|1|…|12|"cancelada"`; nova fase **7 "A finalizar o quadro" / "Finalising the artwork"** inserida entre 6 e a antiga 7; tudo o que estava 7-11 deslocado para 8-12 (`PUBLIC_TIMELINE_PHASES`, `ALL_PUBLIC_PHASES`, `STATUS_TO_PUBLIC_PHASE`, `PUBLIC_PHASE_LABEL_PT/EN`, `PUBLIC_PHASE_COLORS`, `DEFAULT_MESSAGES_PT/EN`). Mensagens default da fase 7 escritas em PT+EN ("A composição foi aprovada! Estamos agora a finalizar o seu quadro com a colagem definitiva, antes de seguir para a moldura." / "Your design has been approved! We are now finalising your artwork with the definitive mounting, before sending it for framing."). Cor da nova fase 7 = rose; `cancelada` mudou de rose para red para evitar colisão. (2) [src/app/(admin)/status/status-client.tsx](src/app/(admin)/status/status-client.tsx) — filtro "esconder concluídas/canceladas" passou a comparar com `phase === 12` em vez de `11`. (3) [_fbr-tracking-updates/utils/supabase.js](_fbr-tracking-updates/utils/supabase.js) — espelhada a mesma mudança (mapeamento, labels PT/EN, mensagens default, e o range `safePhase >= 1 && safePhase <= 12` no `fase_numero`). (4) **Migração `021_public_status_shift_phases.sql`** — re-key idempotente da JSONB `public_status_settings.messages`: chaves 0-6 e "cancelada" mantêm-se, 7-11 copiam para 8-12, a nova chave 7 fica vazia (cai no default de código até a Maria personalizar). Guarda contra duplo-shift verificando se já existe chave "12" antes de mover. **Maria precisa de fazer:** (a) correr `supabase/migrations/021_public_status_shift_phases.sql` no Supabase SQL Editor; (b) se o `fbr-tracking` da sessão 35 já estiver no ar, copiar o `utils/supabase.js` actualizado para o repo `fbr-tracking` e fazer redeploy (caso contrário, basta esperar pela aplicação inicial da sessão 35 — o ficheiro já fica com a versão correcta). Type check (`tsc --noEmit`) e ESLint limpos.

- **Sessão 35 ✅ Fase 6 parte 1 — site público `status.floresabeirario.pt` ligado ao Supabase (substituindo o Excel/Google Sheets manual).** O repo separado [`floresabeirario/fbr-tracking`](https://github.com/floresabeirario/fbr-tracking) (Next.js 13 Pages Router, deployado no Vercel) lia o Google Sheets via service account. Trocado para `@supabase/supabase-js`. **Migração `020_orders_public_status_read.sql`** (já no admin): (1) `GRANT SELECT` **column-level** em `orders` ao role `anon` só para os campos públicos (`id, order_id, customer_name, status, public_status_message_pt/en, public_status_language, estimated_delivery_date, public_status_updated_at`) — sem email, telemóvel, NIF, orçamento, comissão ou notas; (2) policy `orders_public_status_read` com `USING (deleted_at IS NULL AND payment_status <> '100_por_pagar')` — encomendas em pré-reserva continuam invisíveis (cliente nunca recebeu o link antes do 1º pagamento, conforme [[project-status-excel-coexistence]]); (3) `GRANT SELECT` + policy permissiva em `public_status_settings` (singleton com mensagens default globais, sem PII). **Pacote `_fbr-tracking-updates/`** (igual padrão da sessão 21 com fbr-voucher e da sessão 25 com fbr-website) com: (a) `utils/supabase.js` que substitui `utils/googleSheets.js` — mantém o **mesmo contrato de saída** que o sheets antigo (`{id, nome_encomenda, fase, fase_en, fase_numero, mensagem, mensagem_en, ultima_atualizacao, data_entrega}`), por isso `pages/[id].js` só precisa de mudar uma linha (o `import`); inclui port em JS puro de `STATUS_TO_PUBLIC_PHASE`, `PUBLIC_PHASE_LABEL_PT/EN` e `DEFAULT_MESSAGES_PT/EN` (espelho de `src/lib/public-status.ts`); resolve a mensagem efectiva via cadeia override→global default→hardcoded; formata `ultima_atualizacao` como `dd/MM/yyyy, HH:mm` ([[feedback-formato-datas]]) e `data_entrega` como "maio de 2026" / "May 2026" (mês+ano apenas, consistente com `formatPublicEstimatedDelivery` da plataforma); respeita `public_status_language` (pt/en/ambos) populando `fase_en`/`mensagem_en` só quando aplicável (a UI já tem o `isInternational = !fase && !!fase_en`); (b) `pages/api/tracking.js` actualizado (só muda o import path e a validação fica idêntica); (c) `package.json` com `@supabase/supabase-js` em vez de `googleapis`+`google-spreadsheet`; (d) `.env.example` com `SUPABASE_URL`+`SUPABASE_ANON_KEY`; (e) `README.md` com passo-a-passo: correr migração 020 → copiar 3 ficheiros → mudar 1 linha em `[id].js` → `npm install` → env vars no Vercel → push → smoke test (encomenda paga visível, pré-reserva invisível, ID inválido = 404), guia de coexistência com Excel durante a transição, mapping de campos admin→público, troubleshooting (URL com `/rest/v1` duplicado, chave errada, 42501). **Decisão arquitectural reforçada**: a URL pública `https://status.floresabeirario.pt/<order_id>` mantém-se igual; como o `order_id` é o mesmo formato de 16 chars que existia antes, os links já enviados aos clientes continuam a funcionar. **Coexistência**: enquanto houver encomendas activas só no Excel, o Excel continua arquivado para consulta — mas o switch do site é total (Excel deixa de alimentar). Maria precisa migrar manualmente cada encomenda antiga para a plataforma admin antes de descontinuar o Excel.

  **Maria precisa de fazer manualmente quando aceitar este lote:**
  1. **Correr migração 020 no Supabase SQL Editor** (`supabase/migrations/020_orders_public_status_read.sql`)
  2. Clonar o repo `fbr-tracking` localmente; copiar os 3 ficheiros do pacote (`utils/supabase.js`, `pages/api/tracking.js`, `package.json`); apagar `utils/googleSheets.js`; corrigir a linha do import em `pages/[id].js`
  3. `npm install` (substitui googleapis pelo supabase-js)
  4. No Vercel do `fbr-tracking`: adicionar `SUPABASE_URL` (sem `/rest/v1`) e `SUPABASE_ANON_KEY`; remover as antigas `GOOGLE_*` se já não houver outro consumidor; **forçar redeploy** (Vercel não auto-redeploya ao mudar env vars)
  5. Smoke test: encomenda paga visível, pré-reserva (`100_por_pagar`) dá 404, ID inválido dá 404

- **Sessão 34 ✅ Fase 5.5 parte 2 — Métricas com mais cor + Finanças "Competição" + autocomplete de morada nas Parcerias.** Três itens da parte 2 num só passo. **(1) Métricas mais coloridas** ([metricas-client.tsx](src/app/(admin)/metricas/metricas-client.tsx)): header com fundo gradiente rose→amber→emerald; os 4 KPIs principais viraram "hero cards" — cada um com a sua cor temática (Receita do período = emerald, Receita do ano = sky, Encomendas novas = violet, Vales = amber), ícone branco em quadrado colorido, badge de % com cor (emerald/rose/stone consoante sinal). Insights ganham caixa amarela vibrante (gradiente amber→yellow). Receita mensal trocou linha simples por **AreaChart** com gradiente verde (`<defs><linearGradient id="revGradient">…</linearGradient></defs>`). Gráfico de "encomendas por estado" passa a usar **cor por estado** via novo `STATUS_HEX: Record<OrderStatus, string>` em [_styles.ts](src/app/(admin)/preservacao/_styles.ts) (hex correspondente aos `STATUS_DOT_COLORS` em tom 400/500). Os 3 pies (tamanho/fundo/evento) ganham donuts (`innerRadius={32}`, `paddingAngle={2}`) e cada um a sua palette própria (PIE_PALETTE_FRAME violeta→ciano, PIE_PALETTE_BG rosa→amber, PIE_PALETTE_EVENT verde→rosa). Novo gráfico de barras horizontais para "Top 5 canais de aquisição" (com palette ACQ_PALETTE). Mini-KPIs (tempo médio, %extras, canal #1) com ícone colorido (sky/amber/fuchsia). **Top parceiros agora mostra nomes** (não só `partner_id.slice(0,8)…`) — `page.tsx` carrega `partners` em paralelo (`{ id, name, category }`) e passa um `partnerNames: Record<string,string>` ao cliente, que faz lookup e cria link `/parcerias/<id>`. Linhas top-3 mostram ícone `Trophy` colorido (gold/silver/bronze). Tooltips com `borderRadius: 8` e `fontSize: 12`. **(2) Finanças — secção "Competição"** (nova): migração `019_competitors.sql` cria tabela `competitors` (name, websites text[], location_label, latitude/longitude, country default 'PT', prices JSONB com array de `{product, price, notes}`, notes), RLS no padrão admin-escreve/Ana-lê (espelha `vouchers`), audit log, `GRANT SELECT,INSERT,UPDATE,DELETE TO authenticated`. Tipos em [src/types/competitor.ts](src/types/competitor.ts). Server Actions em [src/app/(admin)/financas/actions.ts](src/app/(admin)/financas/actions.ts) com `requireAdmin()`. Página [financas-client.tsx](src/app/(admin)/financas/financas-client.tsx): pill-tabs no estilo Parcerias com 4 secções (Tabela de preços, Despesas, Faturação, Competição) — apenas **Competição** funcional, restantes mostram placeholder "Em construção" elegante; "Competição" abre como default. KPIs sumário (concorrentes registados, preço médio/min/max), search por nome/local/site, cards 2-col com nome+localização+websites (pills sky)+tabela de preços+notas, botão "A partir de" destacado em emerald. Edição inline com formulário expandido (websites múltiplos, lista de preços com produto+preço+notas, país opcional para concorrentes estrangeiros). Botão "Novo concorrente" em violet. Arquivar disponível em cada card. **(3) Parcerias — autocomplete de morada via Nominatim/OpenStreetMap** (gratuito, sem API key, ideal para o uso interno; alternativa pragmática ao Google Maps Places que exige conta de cobrança Google). Novo componente [src/components/ui/address-autocomplete.tsx](src/components/ui/address-autocomplete.tsx): debounce de 400ms (respeita o rate limit de 1/s do Nominatim), Accept-Language pt-PT, `countrycodes=pt` por default, AbortController para cancelar requests obsoletos. Devolve `{label, latitude, longitude}` ao seleccionar. **Padrão "store info from previous renders"** para sincronizar `query` com prop `value` (em vez de `useEffect+setState`, que violaria a regra `react-hooks/set-state-in-effect` — [[feedback-react-set-state-in-effect]]). Workbench da Parceria substitui o trio location_label+latitude+longitude por um único `<AddressAutocomplete>` (novo helper `LocationCard` no fim do file), com toggle "Editar coordenadas manualmente" escondido para casos avançados. Sheet "Novo parceiro" idem. Type check (`tsc --noEmit`) e ESLint limpos.

  **A FAZER NA PARTE 2 (continua):**
  - Aplicar manualmente as 6 mudanças do `_fbr-website-updates/PHASE_5_5_TODO.md` no repo `fbr-website`
  - **Datas em dd/MM/yyyy em toda a stack** — admin OK; pendente confirmação no `fbr-website`

- **Sessão 33 ✅ Fase 5.5 parte 2 — Drag-and-drop entre grupos na tabela de Preservação.** Item 1 da parte 2. Maria pode agora arrastar uma encomenda entre grupos (Pré-reservas, Sem resposta, Reservas, Preservação e design, Finalização, Concluídos, Cancelamentos) para mudar de fase sem abrir o workbench nem usar o select de estado. **Biblioteca**: `@dnd-kit/core@^6.3.1` + `@dnd-kit/utilities@^3.2.2` (standard React, ~12kb, suporta toque + teclado via `PointerSensor` + `KeyboardSensor` com activação por distância de 8px para não conflituar com o click que abre o workbench). **Mudanças concretas:** (1) **`src/lib/supabase/orders.ts`** exporta novo mapa `GROUP_TO_TARGET_STATUS: Record<OrderGroupKey, OrderStatus>` — para cada grupo, o estado natural de chegada (`pre_reservas → entrega_flores_agendar`, `reservas → entrega_agendada`, `preservacao_design → flores_na_prensa`, `finalizacao → a_ser_emoldurado`, `concluidos → quadro_recebido`, `cancelamentos → cancelado`); `sem_resposta` é especial (mantém status base + activa `manually_no_response`). (2) **`OrderRow` em [preservacao-client.tsx](src/app/(admin)/preservacao/preservacao-client.tsx)** ganha nova coluna leading (3% width) com `<button>` arrastável (ícone `GripVertical`, cursor grab/grabbing, `touch-none` para suprimir scroll em PWA mobile); os listeners de pointer ficam SÓ no botão (não na `<tr>`) para o click no resto da linha continuar a abrir o workbench. `useDraggable({ id: order.id, data: { order }, disabled: !canEdit })`. Linha origem ganha `opacity-40` durante o drag. (3) **`GroupSection`** vira drop zone com `useDroppable({ id: droppableId })`; quando `isOver`, mostra ring âmbar + faixa superior "Largar aqui para mover para …"; grupo orfas (estado desconhecido) NÃO é drop zone (passa `droppableId={undefined}` → `disabled: true`). (4) **Vista tabela** envolvida em `<DndContext>` com `sensors`, `onDragStart`, `onDragEnd`, `onDragCancel`; renderiza `<DragOverlay>` com pill flutuante mostrando nome do cliente + order_id. (5) **`handleDragEnd`** trata 3 casos: (a) destino `sem_resposta` → `updateOrderAction({ status: "entrega_flores_agendar", manually_no_response: true, contacted: false })` se vinha de grupo diferente, senão só activa a flag; (b) destino `pre_reservas` → muda status + limpa flag manual (no-op se já lá estava); (c) outros grupos → muda para `GROUP_TO_TARGET_STATUS[target]` e limpa `manually_no_response` defensivamente. A geração de cupão ao chegar a `a_ser_emoldurado` já é tratada pelo próprio `updateOrderAction` (não precisei de mexer). **Restrições:** só admin vê o handle (`canEdit`); viewer continua com modo leitura. Sem confirmação no drop — consistente com o `StatusSelect` actual da tabela (que também muda sem perguntar). **Acessibilidade:** `KeyboardSensor` permite navegação por teclado (Space para pegar, setas para navegar entre droppables, Space para largar); `aria-label` no handle. Type check (`tsc --noEmit`) e ESLint limpos.

- **Sessão 32 ✅ Fase 5.5 parte 2 — Dashboard checklist com 3 caras em vez de dropdown.** Item 1 da parte 2. O `ChecklistCard` em [dashboard-client.tsx](src/app/(admin)/dashboard-client.tsx) tinha um `<Select>` no header para os admins alternarem entre listas — substituído por uma fila de 3 botões redondos com as fotos do António, MJ e Ana (mesmas imagens do login: `/userphotos/{antonio,mj,ana}.webp`). A foto activa tem `ring-2 ring-emerald-600` (cor a condizer com o ícone `ListTodo` verde do card) e `ring-offset-2` para se destacar; as outras duas ficam a 50% de opacidade e voltam a 100% no hover. **Mudanças concretas:** (1) `TEAM_MEMBERS` ganha campo `photo: string` apontando para o ficheiro em `public/userphotos/`; (2) `import Image from "next/image"` adicionado; (3) o `action` do `SectionCard` passa de Select com `labels` para `<div className="flex items-center gap-1.5">` com 3 `<button>` que envolvem `<Image fill sizes="32px" object-cover>` — o `fill` precisa de container `relative` com tamanho fixo (`h-8 w-8 rounded-full overflow-hidden`); (4) `aria-pressed={active}` para acessibilidade. **Comportamento preservado:** só admins vêem os botões (`canSwitchOwner` continua a ser `role === "admin"`); a Ana (viewer) continua a ver apenas a sua checklist, sem botões; o `canWrite` (admin a olhar para checklist alheia) mantém-se a desabilitar inputs como antes. Type check (`tsc --noEmit`) e ESLint limpos.

- **Sessão 31 ✅ Bugfix crítico + rede de segurança replicada em Vale-Presente e Parcerias + grupos fim-de-linha colapsados por default.** Extensão proactiva da sessão 31 depois da Maria dizer "não quero ter que te lembrar destas coisas óbvias". **Vale-Presente (`src/lib/supabase/vouchers.ts` + `vale-presente-client.tsx`):** `groupVouchers` agora usa `PAYMENT_TO_GROUP: Record<VoucherPaymentStatus, VoucherGroupKey>` (compile-time exhaustiveness) e devolve `orfas` para qualquer estado de pagamento desconhecido vindo da BD. Banner vermelho idêntico ao da Preservação + `GroupSection` "Sem grupo (estado desconhecido)" sempre expandido. **Parcerias (`src/lib/supabase/partners.ts` + `parcerias-client.tsx`):** `groupByStatus` agora valida cada `p.status` contra `PARTNER_STATUS_ORDER` antes do `result[p.status].push` (que crashava silenciosamente para estados desconhecidos) e empurra órfãos para `result.orfas`. Novo componente `OrfasGroupSection` (variação do `GroupSection` sem badge de status, com cabeçalho vermelho de alerta). Banner global + secção sempre expandida. **Fim-de-linha colapsados por default:** `Concluídos` e `Cancelamentos` na Preservação, `Rejeitado` nas Parcerias — começam SEMPRE colapsados (mesmo com items dentro), porque são grupos que raramente precisam de atenção diária. O auto-collapse de grupos vazios continua a funcionar para todos. O `changeCategory` das Parcerias também adiciona `rejeitado` ao set de colapsados ao trocar de sub-aba. **Memória:** [[feedback-aplicar-padroes-em-areas-analogas]] — sempre que adicionar uma salvaguarda numa área de listagem agrupada, replicar nas análogas no mesmo passo sem esperar que a Maria peça. Type check (`tsc --noEmit`) e ESLint limpos.

- **Sessão 31 ✅ Bugfix crítico — encomendas com estado `a_finalizar_quadro` desapareciam da Tabela e Cards.** Maria mudou uma encomenda para "A finalizar o quadro" e ela sumiu de todas as vistas com agrupamento. **Causa-raiz:** a migração 018 (Sessão 28) adicionou o estado `a_finalizar_quadro` em quase todos os mapeamentos (`STATUS_LABELS`, `STATUS_COLORS`, `STATUS_ICONS`, `STATUS_TO_PUBLIC_PHASE`, `STATUS_GROUPS` em `_styles.ts`) **mas esqueceu a função `groupOrders` em `src/lib/supabase/orders.ts`** — que é a definição **paralela** dos grupos usada pela vista Tabela e Cards. Como a tabela só renderiza o que vem dos buckets, a encomenda continuava na BD (e visível em Calendário/Timeline, que filtram por deny-list) mas invisível nas duas vistas principais. **Mudanças (estrutura + rede de segurança):** (1) **`src/lib/supabase/orders.ts`**: substituí os 5 filtros independentes por uma única fonte de verdade `STATUS_TO_GROUP: Record<OrderStatus, OrderGroupKey>` — o `Record` força o TypeScript a falhar a compilação se algum dia for esquecido outro estado novo. `groupOrders` agora itera as encomendas uma vez e empurra cada uma para o bucket correspondente; estado desconhecido vai para `buckets.orfas` em vez de evaporar (defensivo contra BD↔código fora de sincronia). Loga `console.error` no browser quando isso acontecer. (2) **`preservacao-client.tsx`**: banner vermelho com `AlertTriangle` no topo de qualquer vista quando `grouped.orfas.length > 0` ("Encomendas com estado desconhecido"). Grupo "Sem grupo (estado desconhecido)" no topo da Tabela e Cards — sempre expandido (não pode ser colapsado), cor vermelha, com ícone de alerta. (3) **`_styles.ts`**: refactor de `STATUS_GROUPS` (array livre, sem garantias) para ser **derivado** de `STATUS_TO_GROUP_LABEL: Record<OrderStatus, StatusGroupLabel>` — segunda barreira de compile-time. Adicionar um estado novo a `OrderStatus` passa agora a quebrar a compilação em dois sítios distintos até ser mapeado. **Confirmação do default da Tabela**: já era o default ([preservacao-client.tsx:550](src/app/(admin)/preservacao/preservacao-client.tsx#L550)), nada a mudar. Type check (`tsc --noEmit`) e ESLint limpos.

- **Sessão 30 ✅ Fase 5.5 parte 2 — alinhamento de colunas das tabelas Vale-Presente e Parcerias com o padrão da Preservação.** Item 6 da parte 2. Auditoria mostrou 6 divergências reais entre Preservação (referência) e as outras 2 listagens: (1) **Forma dos selects inline** — Preservação usa `rounded-md` com `font-semibold` e `hover:brightness-95`; Vale-Presente e Parcerias usavam `rounded-full` com `font-medium` (mais leve, inconsistente). (2) **Comportamento dos grupos vazios** — Preservação permite abrir um grupo vazio (cabeçalho clicável, mostra "Nenhuma encomenda neste grupo." quando aberto); Vale e Parcerias bloqueavam o click (`cursor-default`) e nunca mostravam mensagem. (3) **Padding do header do grupo** — Vale e Parcerias usavam `py-1.5` quando vazio e `py-2.5` quando cheio (a página "saltava" ao abrir/fechar); Preservação usa `py-2.5` constante. (4) **Auto-collapse inicial de grupos vazios** — Preservação faz isso no `useState` initializer; Vale começava com `new Set()` (todos os grupos abertos), Parcerias idem. (5) **Tipografia dos cabeçalhos da tabela de Parcerias** — usava `text-[10px] tracking-wider`; Preservação e Vale usam `text-xs tracking-wide` (consistente). (6) **Padding das células de Parcerias** — `py-2` em todas as células; Preservação e Vale usam `py-1.5` (linhas mais compactas). **Mudanças em Vale-Presente (`vale-presente-client.tsx`):** `PaymentSelect`, `SendSelect`, `UsageSelect` migrados para o template do `StatusSelect`/`PaymentSelect` da Preservação (rounded-md, font-semibold, hover:brightness-95, SelectContent com `rounded-md border-[#E8E0D5]`); `GroupSection` simplificado — removido `effectivelyCollapsed` e o branch `cursor-default`; agora segue exactamente o padrão da Preservação com 3 estados visuais (vazio+colapsado: opacity-60; vazio+aberto: mostra "Nenhum vale neste grupo."; cheio: tabela normal); `useState` initializer dos `collapsedGroups` agora pré-popula com `pre_reservas` e `reservas` se vazios. **Mudanças em Parcerias (`parcerias-client.tsx`):** `StatusSelect` migrado para o mesmo template; `GroupSection` idem (com mensagem "Nenhum parceiro neste estado."); todas as `<td>` da `PartnerRow` passam de `py-2` para `py-1.5`; cabeçalhos `<th>` passam de `text-[10px] tracking-wider` para `text-xs tracking-wide`; texto do grupo vazio "vazio" → "sem parceiros"; auto-collapse inicial via `useState` initializer (calcula vazios da categoria default `wedding_planners`); novo handler `changeCategory(c)` que faz `setActiveCategory(c) + setCollapsedGroups(empty)` para que a troca de tab volte a esconder os grupos vazios da nova categoria — `router.refresh()` não dispara o reset, preservando o open/close do utilizador entre refreshes (escolhi handler em vez de `useEffect` por causa da regra `react-hooks/set-state-in-effect`). Aproveitei para escapar `"{search}"` → `&ldquo;{search}&rdquo;` no empty state da pesquisa (lint pendente). Type check (`tsc --noEmit`) e ESLint limpos.

  **A FAZER NA PARTE 2 (continua):**
  - Drag-and-drop encomendas entre grupos em modo tabela
  - Parcerias: Google Maps autocomplete para morada (em vez de lat/long manual)
  - Dashboard: checklist pessoal com 3 caras em vez de dropdown
  - Métricas: dar muito mais cor à página
  - Finanças: nova secção "Competição" (preços de empresas concorrentes + sites + localização)
  - Form público `fbr-website` (placeholder de `<input type="date">` em EN) — não-prioritário
  - **Datas em dd/MM/yyyy em toda a stack** — admin OK; pendente confirmação no `fbr-website`

- **Sessão 29 ✅ Fase 5.5 parte 2 — refactor do workbench do Vale-Presente para alinhar com o padrão da Preservação.** Maria pediu (na lista grande da sessão 28) para "unificar pagamento+fatura numa secção; reestruturar layout visual no estilo do workbench da Preservação; auditar campos do form vs workbench". **Auditoria** revelou 4 gaps: (a) os campos novos da sessão 28 (`partner_id`, `partner_commission`, `partner_commission_status`, `sticky_note`) não estavam visíveis no workbench, apesar de já estarem na BD via migração 018; (b) faltava indicador de `form_language` (PT/EN — útil para saber em que idioma o cliente preencheu o form); (c) faltava info de RGPD na Metadata; (d) "Pagamento" e "Fatura" estavam em duas Sections separadas. Os campos do form público (remetente, vale, entrega, comentários, origem) já estavam todos visíveis. **Mudanças:** (1) **Header** ganha `Flag` (PT/EN) à direita do código + botão `StickyNoteButton` post-it amarelo (mesmo padrão visual da Preservação — vazio = amarelo claro com "+", com texto = amarelo intenso com preview de 2 linhas, popover com textarea); (2) **Coluna direita reorganizada**: o card "Pagamento" + o card "Fatura" fundiram-se num único "Pagamento e fatura" (Wallet emerald) — o select de pagamento fica no topo, separador, depois NIF Sim/Não com inputs condicionais NIF + anexo Drive; (3) **Novo card "Parceria"** (Handshake orange) com `PartnerCombobox` (cmdk com pesquisa por nome+categoria) + comissão (€) + estado da comissão coloridos — ao escolher um parceiro, auto-preenche `partner_commission = amount * 0.10` (só se ainda vazio) e muda `partner_commission_status` de `na` para `a_aguardar`, igual à Preservação; mostra link directo `/parcerias/<id>` quando há parceiro seleccionado; (4) **Card "Metadata"** ganha rodapé condicional com info de consentimento RGPD (`consent_at`, `consent_version`, `consent_ip`) — só aparece para vales criados via form público (a partir da Fase 5). **`page.tsx`** passa a fazer `Promise.all` de duas queries (voucher + lista de parceiros activos) e passa `partners` ao workbench — mesma estratégia do workbench da Preservação. **Helpers locais**: `StickyNoteButton` e `PartnerCombobox` foram duplicados do workbench da Preservação (são auto-contidos com ~120 linhas no total — extracção para componente partilhado fica para quando houver um terceiro consumidor). Type check (`tsc --noEmit`) e ESLint limpos.

- **Sessão 28 ✅ Fase 5.5 parte 1 — pacote grande de afinações pós-uso.** Maria deu uma lista enorme em conversa única (≈40 itens); organizei como "Fase 5.5" e dividi em parte 1 (esta sessão) e parte 2 (próxima). **Migração 018** (`supabase/migrations/018_fase_5_5_afinacoes.sql`) adiciona: novo estado `a_finalizar_quadro` (entre `a_aguardar_aprovacao` e `a_ser_emoldurado`); colunas `pickup_address/date/time_from/time_to` para recolha no local; `inventory JSONB` (lista de `{qty,name}` por encomenda); `sticky_note TEXT` em orders e vouchers; `payment_40_requested/payment_30_requested BOOLEAN`; `approval_responded BOOLEAN`; nova opção `recomendacao_ia` no CHECK constraint de `how_found_fbr` (orders + vouchers); FK `vouchers.partner_id → partners.id` + `partner_commission` + `partner_commission_status` (vales podem ser recomendados por parceiros tal como encomendas). **Bug do NIF resolvido**: `setLocal(updated)` após o auto-save apagava letras digitadas durante o await; agora preserva qualquer chave em `pendingRef.current`, idem para o reset por prop change. **Bandeira**: emoji `🇵🇹/🇬🇧` falhava em Windows mostrando "PT"/"GB"; novo componente `Flag` em SVG (`src/components/ui/flag.tsx`) usado no workbench. **Sidebar**: click em qualquer zona não-interactiva (fora de `<a>`/`<button>`/`<input>`) expande/colapsa. **Renames**: "Recolha no evento" → "Recolha no local" (workbench + nova-encomenda-sheet + dashboard helper + Order.flower_delivery_method label); "A aguardar" → "Encomenda não paga na totalidade" em `PARTNER_COMMISSION_STATUS_LABELS`. **Sticky note "post-it"**: botão amarelo flutuante (`StickyNoteButton`) no header do workbench — sempre visível, vazio mostra `+ Nota`, com conteúdo mostra preview 2 linhas; click → popover com textarea. **Inventário de flores**: `InventorySection` dentro do card "Flores, quadro e extras", linhas `[qty][nome][×]`, auto-save no blur. **Recolha no local**: quando `flower_delivery_method = recolha_evento`, aparece bloco condicional violet com `pickup_address` (texto livre — Google Maps autocomplete fica para parte 2), `pickup_date` e janela `pickup_time_from → pickup_time_to`. **Cores nos métodos de entrega**: `FLOWER_DELIVERY_METHOD_COLORS` + `FRAME_DELIVERY_METHOD_COLORS` (mãos=emerald, ctt=sky, recolha_evento=violet, nao_sei=stone). **Partner combobox**: substituí Select por `PartnerCombobox` com `cmdk` (pesquisa por nome+categoria) — ao escolher um parceiro, auto-preenche `partner_commission = budget * 0.10` (só se não houvesse valor) e muda o `partner_commission_status` de `na` para `a_aguardar`. **Alertas de pagamento (40%/30%)**: quando se passa para `flores_recebidas`/`flores_na_prensa` (40%) ou `a_ser_fotografado`/`quadro_pronto` (30%) e a flag respectiva ainda for false, abre diálogo amarelo "Pedir Xs%?" com 2 botões ("Lembra-me depois" / "Já pedi"); o segundo marca a flag. **Alerta de aprovação**: quando `status = a_aguardar_aprovacao` AND `!approval_responded`, mostra banner sky (informativo) ou red (urgente, ≥4 dias) com botão "Cliente já respondeu". **Data prevista de entrega** ganha input directo no hero do workbench (antes só editável em `/status`). **Cupão**: removido o texto "Editável para encomendas antigas". **Entrega e feedback** card só aparece quando `status ∈ {quadro_pronto, quadro_enviado, quadro_recebido}`. **Vale-presente icon**: linhas da tabela cujo `gift_voucher_code` corresponde a um vale activo mostram pill amarela "Vale" com link directo `/vale-presente/<code>` (loaded server-side via `voucherCodeToId` map). **Grupos vazios colapsados por default**: `collapsedGroups` inicial inclui qualquer grupo com 0 encomendas, mas continuam abríveis (clicando no header). **Vale-Presente workbench** ganhou edição inline do código (`EditVoucherCode` popover com input mono uppercase) e link directo "Página pública" para `status.floresabeirario.pt/<code>`. **Como conheceu FBR — cores especificadas**: casamentos.pt=`#F16B6B`; Google=fundo branco com letras nas cores do logo (helper `HowFoundFbrLabel`); Vale-Presente=`#6E7DAF`; Florista=rosa pastel "fofo"; nova opção `recomendacao_ia` em gradiente violet→cyan→emerald. **Pacote `_fbr-website-updates`**: `app/_lib/supabase-mappings.js` aceita agora "Recolha no local" (mantendo "Recolha no evento" como fallback) + opção `recomendacao_ia` em ambos os forms; novo ficheiro `PHASE_5_5_TODO.md` lista o que falta fazer manualmente no repo `fbr-website` (placeholder data EN, "we'll always send you", indicativo do telemóvel no form vale, erros explícitos no form vale, "Recolha no local" label, opção IA nos selects). Type check (`tsc --noEmit`) limpo.

  **A FAZER NA PARTE 2 (defer para próxima sessão):**
  - Drag-and-drop encomendas entre grupos em modo tabela
  - Vale-presente: unificar pagamento+fatura numa secção; reestruturar layout visual no estilo do workbench da Preservação; auditar campos do form vs workbench
  - Parcerias: Google Maps autocomplete para morada (em vez de lat/long manual)
  - Dashboard: checklist pessoal com 3 caras em vez de dropdown
  - Métricas: dar muito mais cor à página
  - Finanças: nova secção "Competição" (preços de empresas concorrentes + sites + localização)
  - Transversal: alinhar colunas em todas as tabelas (Vale, Parcerias) como em Preservação
  - Form público `fbr-website` (placeholder de `<input type="date">` em EN): browser-controlled, requer datepicker custom ou `lang="pt"` forçado. **Não-prioritário** (confirmado pela Maria em 2026-05-13) — decidir abordagem antes de implementar (3 opções em `_fbr-website-updates/PHASE_5_5_TODO.md`).
  - **Datas em dd/MM/yyyy em toda a stack** — admin já está OK ([[feedback_formato_datas]]); pendente: confirmar que o site público (`fbr-website`) mostra todas as datas neste formato em PT e EN, incluindo qualquer ecrã do voucher/status público.

  **Coisas que a Maria precisa de fazer manualmente quando aceitar este lote:**
  1. **Correr migração 018 no Supabase SQL Editor** (`supabase/migrations/018_fase_5_5_afinacoes.sql`)
  2. Aplicar as 6 mudanças do `_fbr-website-updates/PHASE_5_5_TODO.md` no repo `fbr-website`
  3. Após push, dar refresh ao admin e verificar visualmente que tudo está OK

- **Sessão 27 ✅ Eliminação de encomendas e vales (arquivar + apagar definitivamente).** Maria precisava conseguir limpar encomendas falsas criadas durante testes do form público. Implementação completa em ambas as abas (Preservação + Vale-Presente):
  - **Server actions** (`preservacao/actions.ts` e `vale-presente/actions.ts`): `restoreOrderAction`/`restoreVoucherAction` (limpa `deleted_at`); `hardDeleteOrderAction`/`hardDeleteVoucherAction` (DELETE real na BD; valida `justification.length >= 3`; insere linha extra no audit_log com a justificação antes do DELETE — o trigger automático já regista o `old_values` da row apagada).
  - **Workbench da Preservação** ganhou botão "Arquivar" no header (a Vale-Presente já tinha) — soft delete via `deleteOrderAction`.
  - **Componente partilhado `src/components/hard-delete-dialog.tsx`**: diálogo com 2 inputs (escrever "APAGAR" + justificação obrigatória), botão de confirmar só fica activo quando ambos válidos. Usado nas duas vistas de arquivados.
  - **Toggle "Arquivados"** no header de cada listagem (Archive icon + contagem). Quando activo: a vista normal (tabela/cards/calendário/timeline) desaparece e mostra-se `ArchivedOrdersView`/`ArchivedVouchersView` — tabela simples com Cliente/Código, data de arquivamento, e dois botões por linha: "Restaurar" (limpa `deleted_at`) e "Apagar" (abre `HardDeleteDialog`). Botão "Nova encomenda"/"Novo vale" esconde-se enquanto o toggle está activo.
  - **`page.tsx` de ambas as abas** faz agora `Promise.all` de duas queries: active (`deleted_at IS NULL`) e archived (`NOT IS NULL`), passa ambas para o cliente.
  - Type check (`tsc --noEmit`) limpo.
- **Sessão 26 ✅ smoke test em preview passou.** Os 4 campos novos (event_type, couple_names cond. casamento, event_location, gift_voucher_code cond. vale-presente) estão a chegar correctamente à BD via o form do `fbr-website` no preview do `develop`. Pendente igual à sessão 25: merge `develop` → `main` no fbr-website para ir a produção em `floresabeirario.pt`.
- **Sessão 26 (campos em falta no form de Reserva):** ao reler o `public/plataforma admin.pdf`, descobrimos 4 campos que o PDF spec inclui mas o form público nunca recolheu — `event_type` (Tipo de evento), `couple_names` (Nome dos noivos, condicional a Casamento), `event_location` (Localização do evento) e `gift_voucher_code` (Código Vale-Presente, condicional a "Ofereceram-me um Vale-Presente"). As 4 colunas já existiam na tabela `orders` desde a migração 001 — só faltavam ser recolhidas. **Pacote actualizado em `_fbr-website-updates/`:** novo `app/reservar-preservacao/ReservarPreservacaoForm.jsx`, novo `messages/additions.md` com snippets PT/EN para colar em `messages/{pt,en}.json`, `_lib/supabase-mappings.js` actualizado (mapping `TIPO_EVENTO`, validação extra para `tipoEvento`/`nomeNoivos`/`codigoValePresente`/`localEvento`, descarta `gift_voucher_code` quando `how_found_fbr ≠ vale_presente` e `couple_names` quando `event_type ≠ casamento` por defesa em profundidade), `api/reservar-preservacao/route.js` com `MAX_LENGTHS` para os 3 campos de texto livre novos e linhas extra na notificação Resend. Decisão da Maria: o cliente **não** especifica qual cor quando escolhe `tipoFundo = Cor` — guardado em memória `feedback_form_publico_cor_sem_especificar.md`. Migração 016 não precisa de mexer (a policy `orders_public_insert` só restringe campos admin; os 4 campos novos passam). README actualizado com novo passo de smoke test que confirma os condicionais.
- **Fase 5 fechada (sessão 25):** os formulários públicos do site `floresabeirario.pt` deixam de gravar no Monday e passam a gravar directamente no Supabase. ✅ Smoke test no preview (`fbr-website-git-develop-…vercel.app`) passou para ambos os forms — **Reserva** e **Vale-Presente**. Migrações 016 + 017 já estão em produção no Supabase. **Pendente** (decisão da Maria sobre o momento): merge `develop` → `main` no fbr-website para o switch ir a `floresabeirario.pt` em produção, smoke test em produção, e remover env vars do Monday.

## Próximo passo CONCRETO
**Sessão 44 (pesquisa global Cmd+K) — passos manuais da Maria:**
1. Não há migração nem env vars
2. Push para Vercel
3. Smoke test: `Ctrl+K` abre palette; escrever ≥2 chars devolve hits agrupados; click navega para o workbench; testa em encomendas/vales/parceiros/ideias/receitas; Ana também consegue.

**Sessão 43 (análise sessão 42 atacada + ideias futuras promovidas) — passos manuais da Maria:**
1. **Correr migrações 028, 029, 030** (por esta ordem) no Supabase SQL Editor:
   - `028_recipes.sql` — Livro de Receitas (wiki por tipo de flor)
   - `029_chat_messages.sql` — Chat interno (versão mínima: só texto + Realtime)
   - `030_expenses.sql` — Tracking de despesas em Finanças
2. (Se ainda não feitas) Correr 025, 026, 027 (sessão 42) e 024 (sessão 41)
3. Push para Vercel
4. Smoke test:
   - **Comissão condicional**: abrir workbench de uma encomenda sem parceiro — campos Comissão (€) e Estado da comissão devem estar escondidos. Idem para vales sem parceiro.
   - **Export CSV de vales**: ir a Vale-Presente, clicar "Exportar" — descarrega CSV pt-PT (Excel).
   - **Export CSV de parceiros**: ir a Parcerias, clicar "Exportar" — descarrega só os parceiros da categoria activa.
   - **Livro de Receitas**: criar receita "Rosa", abrir, adicionar passos, observações, foto (URL Drive), confirmar auto-save.
   - **Chat interno**: abrir num separador, com a Ana noutro — mensagem da MJ aparece na Ana em tempo real (Realtime).
   - **Finanças → Despesas**: registar uma despesa, mudar categoria/método, arquivar.
   - **Finanças → Faturação**: confirmar KPIs (receita do mês, do ano, despesas, lucro) + gráfico de barras 12 meses.
   - **Healthchecks**: ver verificações por categoria, filtros por estado, hints accionáveis.
   - **RGPD → Descarregar PDF**: pesquisar cliente, clicar "Descarregar PDF" — abre print dialog automaticamente.

**Próximas sessões (44+) — restantes itens da Fase 6** (deixados para o fim — [[feedback-fase6-ordem-integracoes]]):
- **Gmail API** — histórico de emails por encomenda no workbench (placeholder na sessão 5; foundation OAuth já está pronta desde a sessão 37)
- **WhatsApp manual** — screenshot/texto no workbench (placeholder; sem API oficial — fica manual)
- **Anthropic API** — assistente de resposta AI no workbench (placeholder na sessão 7)
- **Chat interno — media** (foto, vídeo, áudio): a versão da sessão 43 só tem texto. Adicionar upload + gravação no browser fica para sessão dedicada.

**Outros pendentes:**
- Pesquisa global (Cmd+K) — ✅ feita na sessão 44
- Backup automático diário para Google Drive — DECISÃO da Maria: não é necessário
- 6 mudanças no `fbr-website` em `_fbr-website-updates/PHASE_5_5_TODO.md` (feito? a Maria disse "D feito" na sessão 43)

**Sessão 36 — fix da fase pública "A finalizar o quadro":**
- Correr `supabase/migrations/021_public_status_shift_phases.sql` no Supabase SQL Editor (faz shift dos overrides existentes em `public_status_settings.messages`).
- Se o `fbr-tracking` (sessão 35) já estiver em produção, copiar a versão actualizada de `_fbr-tracking-updates/utils/supabase.js` para o repo `fbr-tracking` e forçar redeploy.

**Fase 6 parte 1 (sessão 35) — aplicar manualmente no repo `fbr-tracking`**

Ver `_fbr-tracking-updates/README.md` para o passo-a-passo completo. Resumo:
1. Correr `supabase/migrations/020_orders_public_status_read.sql` no Supabase SQL Editor
2. No clone local de `fbr-tracking`: copiar `utils/supabase.js`, `pages/api/tracking.js`, `package.json`; apagar `utils/googleSheets.js`; mudar 1 linha em `pages/[id].js`
3. `npm install`
4. Vercel do `fbr-tracking`: adicionar `SUPABASE_URL`+`SUPABASE_ANON_KEY` e forçar redeploy
5. Smoke test: encomenda paga visível; pré-reserva e ID inválido dão 404

⚠ **Sessão 35:** correr `supabase/migrations/020_orders_public_status_read.sql`. Dá GRANT SELECT column-level aos campos públicos de `orders` ao role `anon` + policy `orders_public_status_read` (paga e não-arquivada) + leitura de `public_status_settings`.

**Fase 5.5 parte 2 — falta apenas:**
1. Aplicar manualmente as 6 mudanças do form público `fbr-website` (`_fbr-website-updates/PHASE_5_5_TODO.md`)
2. Confirmar formato de datas em todo o `fbr-website` (PT + EN)

⚠ **Sessão 34:** correr `supabase/migrations/019_competitors.sql` no Supabase SQL Editor. Cria a tabela `competitors` (nome, websites, localização, prices JSONB, country, notes) com RLS admin-escreve/Ana-lê, audit log, e GRANTs ao role `authenticated`.

**Fase 6 — restantes itens (a seguir ao status):**
1. Gmail API — histórico de emails por encomenda no workbench (placeholder na sessão 5)
2. Google Drive API — auto-criação da pasta do cliente ao 1º pagamento (memória `project_drive_auto_creation.md`)
3. Google Calendar API — criar evento na data do evento ao confirmar pagamento
4. Anthropic API — assistente de resposta AI no workbench (placeholder na sessão 7)
5. PWA — manifesto + service worker para uso no telemóvel
6. RGPD avançado — exportação de dados pessoais por cliente (PDF/JSON), retenção de 10 anos com alertas

⚠ **Sessão 28 (Fase 5.5 parte 1):** correr `supabase/migrations/018_fase_5_5_afinacoes.sql` no Supabase SQL Editor. Adiciona novo estado `a_finalizar_quadro`, campos de recolha no local, inventário JSONB, sticky notes, flags 40%/30%/aprovação, parceiros em vouchers, e opção `recomendacao_ia`.

✅ **Migrações 013 + 015 + 014 corridas com sucesso (2026-05-08).** Parceiros do Monday importados — 171 parceiros, 232 interações, 136 acções pendentes. `phones` agora em JSONB com etiquetas (4 parceiros com labels reais extraídas; 14 fillers tipo "outro telemovel" reconhecidos).

⚠ **Sessão 25:** correr `supabase/migrations/016_public_form_inserts.sql` E `supabase/migrations/017_orders_anon_returning.sql` no Supabase SQL Editor (por esta ordem). A 017 é hotfix necessária descoberta no smoke test — sem ela o INSERT do form falha com `42501 — permission denied for table orders` porque o cliente supabase-js usa `INSERT...RETURNING` que requer GRANT SELECT (a 016 só fez GRANT INSERT).

⚠ **Antes de começar a próxima sessão**, executar `supabase/migrations/012_tasks_and_checklist.sql` no Supabase SQL Editor (cria tabelas `tasks` e `personal_checklist`, RLS, audit log, GRANTs).

⚠ **Antes de começar a próxima sessão**, executar `supabase/migrations/009_create_vouchers.sql` no Supabase SQL Editor (cria tabela vouchers, trigger de código de 6 dígitos, RLS admin/viewer, audit log).

⚠ **Antes de começar a próxima sessão**, executar `supabase/migrations/005_public_status.sql` no Supabase SQL Editor (adiciona colunas `public_status_message_pt/en`, `public_status_language`, `estimated_delivery_date`, `public_status_updated_at`, trigger `sync_public_status_fields`, e tabela `public_status_settings`).

✅ **Importação histórica feita (2026-05-04):** as 17 encomendas do Monday foram importadas via `supabase/migrations/006_import_monday.sql`. Sandra, João e "Teste Noiva" excluídos (manuais/teste). Migração já corrida — não voltar a correr.

⚠ **Sessão 16:** correr `supabase/migrations/007_manually_no_response.sql` no SQL Editor — adiciona coluna `manually_no_response` (boolean default false) para permitir mover manualmente uma pré-reserva para "Sem resposta".

⚠ **Sessão 17:** correr `supabase/migrations/008_remove_30_por_pagar.sql` no SQL Editor — remove o estado de pagamento `30_por_pagar` (converte registos existentes em `70_pago`) e ajusta o CHECK constraint.

⚠ **Pendente para a Fase 5 (formulário público):** quando o cliente escolher "Florista" no campo "Como conheceu a FBR", tem de aparecer uma caixa obrigatória "Que florista?" (já implementado nos formulários admin: nova encomenda + workbench).

## Ideias futuras / Pendências (a planear)

- **Chat interno entre membros (António, MJ, Ana):** canal de comunicação dentro da plataforma com texto, vídeo, foto e áudio — substituir trocas avulsas por WhatsApp/email para tudo que diz respeito à operação. (Provável Fase 6 ou 7.)
- **Nova aba "Livro de Receitas 👩‍🍳":** secção tipo wiki para registar o "how-to" da preservação por tipo de flor (rosa, peónia, silvestre, etc.). Objectivo: guardar conhecimento adquirido e evitar perdê-lo. Cada receita com fotos, passos, tempo de prensa, observações.
- **Aba Parcerias — comissão condicional:** quando o parceiro recomendador for "Nenhum parceiro", esconder os campos *Comissão (€)* e *Estado da comissão* no workbench (ficam só ruído). Implementar quando a aba Parcerias existir (Fase 5).

**Decisão arquitetural confirmada (2026-05-03):** todos os ficheiros relacionados com encomendas (fotos, comprovativos, faturas, inspirações) são guardados no Google Drive, na pasta do cliente. A plataforma só guarda o URL/link. Não usar Supabase Storage para isto.

⚠ **Sessão 20:** correr `supabase/migrations/011_vouchers_fix_and_extra_fields.sql` no SQL Editor — (1) corrige o **erro 500 ao criar vale** (faltavam GRANTs em `vouchers` para o role `authenticated`); (2) adiciona 3 campos do PDF: `recipient_contact` (email/WhatsApp do destinatário), `recipient_address` (morada para envio físico) e `ideal_send_date` (data ideal de envio).

## Notas de sessão
- **2026-05-08 (sessão 26, campos em falta no form de Reserva):** Ao reler o `public/plataforma admin.pdf` confirmaram-se 4 campos do `(user)` que o form do site nunca recolhia: `event_type`, `couple_names` (cond. casamento), `event_location` e `gift_voucher_code` (cond. how_found_fbr=vale_presente). Pacote `_fbr-website-updates/` actualizado: (a) `ReservarPreservacaoForm.jsx` com `tipoEvento` (select obrigatório), `nomeNoivos` (input cond.), `localEvento` (input obrigatório), `codigoValePresente` (input cond. obrigatório). Os condicionais limpam-se ao mudar o pai (e.g. mudar `tipoEvento` de Casamento limpa `nomeNoivos`); (b) `messages/additions.md` lista snippets PT/EN prontos a colar dentro de `formReserva` em cada um dos `messages/{pt,en}.json` do `fbr-website` (mantendo o padrão "valor em PT, label traduzido"); (c) `_lib/supabase-mappings.js` ganha `TIPO_EVENTO` e descarta `gift_voucher_code`/`couple_names` em silêncio quando o campo pai não é o esperado (defesa em profundidade — mesmo que alguém modifique o JS); (d) `api/reservar-preservacao/route.js` com `MAX_LENGTHS` para os 3 textos novos e linhas extra na notificação Resend; (e) README com novo passo do smoke test que pede para confirmar os condicionais. **Decisão da Maria** (registada via AskUserQuestion + memória): quando `tipoFundo = Cor`, NÃO mostrar campo "qual cor?" ao cliente — a cor é discutida na conversa de confirmação. Migração 016 não precisa de mexer (policy `orders_public_insert` não bloqueia estes campos). **Lição:** a lista "comoConheceuOpcoes" no form actual já tinha as 8 opções do PDF — o problema era só o campo do código que aparecia condicionalmente.
- **2026-05-08 (sessão 25, smoke test):** **Forms públicos a funcionar em produção.** Smoke test do preview deployment do `develop` no Vercel correu para 4 obstáculos sequenciais antes de funcionar — todos relacionados com configuração, não com código. **Obstáculo 1 — env vars não chegavam**: a `SUPABASE_ANON_KEY` foi adicionada DEPOIS do primeiro deploy, e adicionar/editar env vars no Vercel não dispara redeploy automático. Solução: redeploy do branch develop (sem cache). **Obstáculo 2 — `/rest/v1` duplicado**: a Maria colou no campo `SUPABASE_URL` o REST endpoint completo do Supabase (`https://xxx.supabase.co/rest/v1`) em vez do Project URL. O cliente supabase-js já adiciona `/rest/v1/orders` automaticamente, então ficava `https://xxx.supabase.co/rest/v1/rest/v1/orders` → 404 com `code: PGRST125, message: 'Invalid path specified in request URL'`. Solução: editar a env var e deixar só `https://xxx.supabase.co`. **Obstáculo 3 — chave anon errada**: a Maria tinha colado uma chave que não era a anon (provavelmente service_role ou expirada) → 401 Unauthorized. Solução: re-copiar do Supabase Dashboard → Settings → API → "anon public" (a que começa com `eyJ`). **Obstáculo 4 (o subtil) — RETURNING precisa de GRANT SELECT**: a migração 016 só fez `GRANT INSERT TO anon` mas o cliente `supabase-js` usa `INSERT...RETURNING` por baixo (porque o `.insert(payload).select("id, order_id").single()` quer devolver o registo inserido). O PostgREST avalia o RETURNING como SELECT, que requer permissão SELECT. Erro: `code: '42501', message: 'permission denied for table orders'`. **Hotfix → migração `017_orders_anon_returning.sql`**: GRANT SELECT column-level apenas em `(id, order_id)` e `(id, code)`, mais policies SELECT muito apertadas (`USING (created_at >= NOW() - INTERVAL '5 seconds')`) — só permite ler registos acabados de inserir, sem expor PII nem dados antigos. Ambas as tabelas (orders e vouchers) cobertas. Após a 017 correr, primeira reserva entrou no Supabase com sucesso. **Lições registadas:** (1) Vercel não faz redeploy automático ao adicionar env vars — sempre forçar; (2) SUPABASE_URL deve ser sempre apenas `https://<id>.supabase.co`, sem `/rest/v1` (o cliente JS adiciona); (3) supabase-js `INSERT...RETURNING` requer GRANT SELECT mesmo quando só queres inserir — solução com policy de tempo curto; (4) erro 401 vs 403 distingue chave inválida (401) de RLS rejeição (403). README do `_fbr-website-updates/` actualizado para mencionar a migração 017 explicitamente.
- **2026-05-08 (sessão 25):** **Fase 5 (parte 2/2) — formulários públicos migrados de Monday → Supabase.** Investiguei o repo `floresabeirario/fbr-website` (Next.js 16, next-intl PT/EN, deployado em produção) e descobri que ele já tinha honeypot, rate limit, validações e checkbox de termos — só faltava trocar o destino dos dados. **Migração `016_public_form_inserts.sql`**: (1) adiciona colunas `consent_at`/`consent_version`/`consent_ip` a `orders` e `vouchers` (RGPD); (2) `GRANT INSERT ON orders/vouchers/audit_log TO anon` e `GRANT EXECUTE ON generate_voucher_code/update_updated_at TO anon`; (3) **policies `orders_public_insert` e `vouchers_public_insert`** com `WITH CHECK` muito apertado: forçam `consent_at IS NOT NULL` e bloqueiam o role anon a alterar campos admin (status tem de ser `entrega_flores_agendar`, payment `100_por_pagar`, contacted=false, partner_id null, etc.). Mesmo que alguém modifique o JS do site para tentar criar uma encomenda já paga, a BD rejeita. **Pacote `_fbr-website-updates/`** (igual padrão da sessão 21 com fbr-voucher) com: (a) `app/_lib/supabase-mappings.js` que mapeia as strings `valor` dos forms (são iguais em PT e EN — só os `label`s mudam) para os enums internos da BD; (b) `app/_lib/turnstile.js` com verificação opcional do Cloudflare Turnstile (activa-se só se `TURNSTILE_SECRET` existir, no-op caso contrário — fica preparado mas inactivo); (c) `app/api/reservar-preservacao/route.js` e `app/api/vale-presente/route.js` reescritos para usar `@supabase/supabase-js` em vez do GraphQL do Monday — mantêm honeypot, rate limit, validações, max_lengths e a notificação Resend interna para `info@floresabeirario.pt` (que continua a avisar quando entra reserva); guardam `consent_at`/`consent_version`/`consent_ip`; reservas entram com `consent_version="1.0-explicit"`, vales com `"1.0-implicit"` (TODO: adicionar checkbox de termos ao form de vale numa próxima sessão); (d) `README.md` com checklist passo-a-passo (correr migração → env vars no Vercel → copiar 4 ficheiros → `npm install @supabase/supabase-js` → push → smoke test → opcional remover Monday env vars), guia de Cloudflare Turnstile para mais tarde, secção troubleshooting. **Tipos do admin** (`src/types/database.ts` e `src/types/voucher.ts`) ganharam os 3 campos de consentimento. **Decisão da Maria** registada via AskUserQuestion: sem email automático ao cliente — só nota interna via Resend (já existia); confirmação visual no ecrã basta. **Lições:** (1) os "valor" dos forms em PT e EN são idênticos — o i18n só muda labels, simplifica imenso; (2) o role `anon` precisa de `GRANT INSERT` na tabela E na `audit_log` (senão o trigger rebenta a inserção), e de `EXECUTE` em todas as funções usadas em DEFAULTs ou triggers; (3) o programador anterior já tinha implementado a maior parte das protecções (descobri-o no clone do repo, contrário à expectativa inicial da Maria) — só faltava trocar o destino. Type check (`tsc --noEmit`) limpo.
- **2026-05-08 (sessão 24, parte 2):** **Telemóveis dos parceiros ganham etiqueta.** A MJ pediu para conseguir associar cada telemóvel a uma pessoa/departamento (ex.: "Ana Paula - 915704383, Diogo - 933886082") em vez de só números soltos. **Mudança de schema**: `partners.phones` deixou de ser `TEXT[]` e passou a `JSONB` com `[{label, number}]`. **Lição**: a 013 já tinha sido corrida (a MJ disse "não corri nada desta sessão" mas a 013 já estava em produção). Tentei editar a 013 directamente — não teve efeito porque `CREATE TABLE IF NOT EXISTS` ignora tabelas existentes. Resultado: erro `42804: column phones is of type text[] but expression is of type jsonb` ao correr a 014. **Solução final**: 013 reposta ao estado original (TEXT[]) com nota a apontar para a 015; criada **`015_partner_phones_with_labels.sql`** idempotente que faz `ALTER COLUMN phones TYPE JSONB USING (...)` convertendo cada string em `{label: null, number: string}`. Ordem de execução: 013 → 015 → 014. **Tipo novo** `PartnerPhone = {label: string|null, number: string}` em `src/types/partner.ts`. **UI actualizada** em 3 sítios: (1) `PhonesField` no workbench da Parceria — 2 inputs por linha (etiqueta + número) com auto-save no blur; (2) display inline na listagem `parcerias-client.tsx` mostra `Etiqueta: número` quando há label; (3) `NovoParceiroSheet` com 2 inputs no formulário de criação. **Helper `searchPartners`** procura agora também em `phone.label`. **Importador actualizado** para parsear labels das notas do Monday: regex divide por `|`/`\n`/`;`, captura label imediatamente antes do número, descarta filler ("outro telemovel", "alt tele", "atl.tele", etc.) marcando label=null. Resultado: 4 parceiros com labels reais extraídas (R Concept → Rita Abecasis + Margarida Espiñeira; NOMAD eventos → Inês S. Freitas + Inês Costa F; Flores de Coimbra → Ana paula + Diogo). 14 outros casos de "outro telefone" bem identificados como filler.
- **2026-05-08 (sessão 24):** **Importação dos parceiros do Monday.** A MJ adicionou 4 ficheiros Excel à `public/`: `Wedding_Planners`, `Floristas`, `Quintas_de_Eventos`, `Outros_contactos`. Cada Excel tem folha principal (lista de parceiros) + folha "updates" (histórico). Scripts: `scripts/parse-monday-parceiros.js` (lê os 4 xlsx → JSON) e `scripts/import-monday-parceiros.js` (JSON → SQL). Migração gerada: `supabase/migrations/014_import_monday_partners.sql` (2622 linhas). **Totais**: 171 parceiros, 232 interações, 136 acções pendentes, 18 telemóveis extras extraídos das notas. **Decisões da MJ** (registadas via AskUserQuestion): (1) estado "Contactado 🌼" do Monday → `tentativa_contacto` na plataforma; (2) coluna "Ações" → 1 acção pendente por parceiro com `assignee_email=null`, `due_date=null` ("Nada a fazer" ignorada); (3) canal das interações inferido do texto (telefone/whatsapp/email/reuniao por primeira ocorrência da palavra-chave; senão "outro"); (4) telemóveis adicionais extraídos das notas com regex tolerante a espaços/dashes/+351 — apanhou casos como "outro telemovel:  911835704", "Ana paula - 915 704 383 | Diogo - 933886082". **Comissão**: "Sim"→sim, "Não"→nao, "Sem informação"/"Ñ ficou claro"→a_confirmar. **Updates**: todos os 232 estão no Monday como autor "Maria J. Brito" → `by = info+mj@floresabeirario.pt`. Datas convertidas de "18/January/2026 07:58:32 PM" para ISO via parser custom. ⚠ Pré-requisito: migração 013 (parceiros) tem de ser corrida ANTES da 014.
- **2026-05-08 (sessão 23):** **Fase 5 (parte 1) — Aba Parcerias completa.** Migração `013_partners.sql`: tabela `partners` com nome, categoria (wedding_planners/floristas/quintas_eventos/outros), estado de funil de 6 fases (`por_contactar`/`pendente`/`tentativa_contacto`/`aceite`/`confirmado`/`rejeitado`), pessoa responsável, email, telemóveis e links em arrays, localização (label + lat/long para o mapa), `accepts_commission` (sim/não/a_confirmar), notas, `interactions JSONB` (histórico append-only com canal email/whatsapp/telefone/reuniao/outro), `actions JSONB` (acções assignáveis com due_date e done). **RLS especial** — os 3 utilizadores (incluindo a Ana, viewer noutras tabelas) têm escrita completa, conforme spec. GRANT explícito (lição da migração 003 e 011) + audit log. Adiciona FK real `orders.partner_id → partners.id` (a coluna existia desde 001 mas sem FK) e cria a coluna `vouchers.partner_id`. **Tipos** em `src/types/partner.ts` com labels PT, cores por estado, ordem das categorias e dos estados. **Helpers** em `src/lib/supabase/partners.ts` (filterByCategory, groupByStatus, partnerStats, searchPartners). **Server Actions** em `src/app/(admin)/parcerias/actions.ts` — usam `requireUser()` em vez de `requireAdmin()` para deixar a Ana editar; manipulação dedicada de interações e acções (read-modify-write em arrays JSON, com email do user gravado na criação). **Listagem (`/parcerias`)**: pill-tabs por categoria com contagem · view switcher Tabela/Mapa · search global · grupos colapsáveis por estado · cada linha mostra contacto inline, local, badge de comissão, contagem de clientes recomendados (orders+vouchers do parceiro) e acções pendentes · select de estado inline com cores · empty states diferenciados (sem nenhum vs categoria vazia). **Mapa de Portugal** (`portugal-map.tsx`): SVG inline com silhueta simplificada do continente desenhada a partir de pontos lat/long reais; cidades de referência (Porto, Coimbra, Lisboa, Évora, Faro); pontos coloridos por estado clicáveis com tooltip; legenda lateral com contagem por estado e aviso de parceiros sem coordenadas. **Workbench (`/parcerias/[id]`)**: 3 colunas — esquerda sticky com **Histórico de interações** (registar com canal+datetime+sumário, lista cronológica, autor inline) e **Acções pendentes** (assignee dos 3 utilizadores, due_date com aviso overdue, secção "feitas" colapsável); meio com Identificação (nome+categoria+estado), Contacto (responsável, email, telemóveis múltiplos editáveis, links múltiplos), Localização (label + lat/long), Comissão e Notas — tudo com auto-save no blur (padrão da aba Vale-Presente); direita com **Clientes recomendados** (encomendas + vales agrupados por tipo, com link para o respectivo workbench, total e receita acumulada) e Metadata. **Ligação à Preservação:** workbench da encomenda agora carrega a lista de parceiros (mínimo: id, nome, categoria, estado) e mostra um select activo com link "abrir parceiro" quando há um seleccionado. **Lições:** o `Select` do base-ui aceita `string | null` no `onValueChange`, então não dá para passar directamente um `setState<string>` — embrulhar com `(v) => setX(v ?? "default")`. Build do Next + tsc limpos.
- **2026-05-07 (sessão 22):** **Fase 4 fechada — Dashboard + Tarefas + Métricas.** Migração `012_tasks_and_checklist.sql`: tabela `tasks` (afazeres globais com assignee_email, priority baixa/média/alta/urgente, due_date, done + done_at + done_by, ligação opcional a `orders.id`) e tabela `personal_checklist` (checklist privada por utilizador, com owner_email + position drag-and-drop ready). RLS: tasks acessíveis aos 3 utilizadores (Ana inclusive, conforme spec — "viewer pode editar tarefas"); checklist é só do dono ou dos admins. Audit log activado em `tasks`. GRANT SELECT/INSERT/UPDATE/DELETE em ambas (lição da sessão 4 e 20). Tipos em `src/types/tasks.ts`. Server Actions em `src/app/(admin)/actions.ts` usam novo `requireUser()` (em vez de `requireAdmin()`) para deixar a Ana criar/editar. **Página Dashboard** (`src/app/(admin)/page.tsx` + `dashboard-client.tsx`): grid 2×2 com 4 secções — (1) **Checklist pessoal**: admins podem alternar entre as listas dos 3 utilizadores via select; viewer só vê a sua; toggle done com indicador visual, delete inline em hover; form de adicionar no rodapé; (2) **Afazeres globais**: filtros Todas/Minhas/Feitas; criação inline com sheet expansível (título + assignee + prioridade + prazo); cada linha mostra prioridade colorida (slate→sky→amber→rose), assignee inline editável e badge de prazo (rosa se overdue); (3) **Recolhas e entregas (próximos 30 dias)**: alimentado pelos métodos de envio CTT/recolha; cada linha clica para o workbench; cores azul/violeta/verde por tipo; (4) **Alertas**: eventos ≤7 dias, encomendas paradas ≥14 dias, pré-reservas sem contacto ≥4 dias, vales a expirar ≤3 meses. Helpers em `src/lib/dashboard.ts`. Botão "Ver métricas" no header. **Aba Métricas** (`/metricas`, indentada como sub-item de Dashboard no sidebar, novo ícone LineChart): filtros por preset (este mês / mês passado / 3m / 6m / este ano / ano passado / personalizado); KPIs principais (Receita do período + variação %, Receita do ano + variação anual, Encomendas novas + variação mensal, Vales vendidos + % conversão); insights automáticos com Lightbulb; gráfico de receita mensal (linha, 12m); barras horizontais de encomendas por estado; 3 pies (tamanho de moldura, fundo, tipo de evento); KPIs adicionais (tempo médio de conclusão global vs. últimos 6m, % com extras, top canais); tabela top 5 parceiros (com placeholder a dizer que os nomes vêm na Fase 5). Cálculos em `src/lib/metrics.ts` puros (sem chamadas BD) — recebem orders + vouchers + range. Receita inclui orders com pagamento ≥30% e vales pagos não-convertidos (evita dupla contagem). Recharts já estava instalado. Build OK, sem erros TypeScript. **Lição:** o `label` do `<Pie>` do recharts esperava `(props: PieLabelRenderProps) => ReactNode` — destructuring `{ name, value }` em vez de tipar manualmente.
- **2026-05-07 (sessão 21):** **Fase 3 fechada — site público `voucher.floresabeirario.pt` ligado ao Supabase.** Repo separado `floresabeirario/fbr-voucher` (HTML estático + 2 Vercel Serverless Functions). Substituídos `api/voucher.js` e `api/share.js` para trocar Google Sheets API por `@supabase/supabase-js`; `package.json` agora só tem `@supabase/supabase-js` (removido `googleapis`). API mantém os mesmos campos de output (`codigo`, `remetente`, `destinatario`, `valor`, `mensagem`, `validade`) para o `index.html` continuar a funcionar sem alterações; valor formatado em euros pt-PT, validade em `dd/MM/yyyy`. **Migração `010_vouchers_public_read.sql`**: `GRANT SELECT ON vouchers TO anon` + `CREATE POLICY vouchers_public_read FOR SELECT TO anon USING (deleted_at IS NULL AND payment_status = '100_pago')` — só vales pagos e não arquivados ficam visíveis publicamente. Env vars no Vercel do `fbr-voucher`: `SUPABASE_URL` + `SUPABASE_ANON_KEY` (sem prefixo `NEXT_PUBLIC_`, é Node puro). Pasta `_fbr-voucher-updates/` na raiz do admin guarda os ficheiros prontos a copiar para o outro repo (apenas referência). **Lição aprendida**: a primeira tentativa da migração 010 só criava a policy, mas faltava o `GRANT SELECT ON vouchers TO anon` — em Postgres + Supabase é preciso ambos (RLS sozinha não é suficiente sem o GRANT base). Diagnóstico foi feito com queries a `pg_policies` e `information_schema.role_table_grants`, e o REST testado via PowerShell `Invoke-RestMethod` para isolar Supabase vs Vercel.
- **2026-05-07 (sessão 20):** Vale-Presente — alinhamento com o PDF spec. (1) **Bug crítico do "erro ao guardar"**: a migração 009 criou a tabela `vouchers` com RLS+políticas mas **esqueceu o `GRANT SELECT/INSERT/UPDATE/DELETE`** ao role `authenticated` (mesma armadilha da sessão 4 com orders). Migração `011` corrige isso. (2) **Campos novos**: `recipient_contact`, `recipient_address`, `ideal_send_date` — só visíveis quando o vale é entregue diretamente ao destinatário, com regras condicionais (digital → contacto; físico → morada; ambas → data ideal). (3) **Labels alinhados ao PDF**: "Dados do remetente"; "Como prefere ser contactado/a?"; "E-mail de contacto" sempre obrigatório com helper "Pedimos um e-mail como contacto alternativo"; "Nome da(s) pessoa(s) a quem se destina" com helper "Este nome será utilizado para personalizar o vale"; "Valor do vale (€)" com erro "O valor mínimo é de 300€, correspondente ao quadro mais pequeno"; "Quero que o vale seja entregue a"/"Tipo de vale" com opções literais do PDF ("Mim (remetente)", "Diretamente ao destinatário", "Digital — por email ou WhatsApp (gratuito)", "Físico — cartão com envelope (9€ + portes)"); "Comentários ou pedidos especiais" com texto sobre 3 dias úteis. (4) **Validações**: telemóvel obrigatório só se contacto preferido = WhatsApp; campos do destinatário obrigatórios quando vai diretamente; data ideal não pode ser anterior a hoje; contadores 1000 chars na mensagem e comentários. (5) **Reporte de erro**: o `setFieldErrors({_root})` agora inclui a mensagem real (`err.message`) em vez de "Tenta novamente" genérico. (6) **Workbench**: novos campos aparecem na secção Entrega quando aplicáveis.
- **2026-05-07 (sessão 19):** Afinações pós-revisão. (1) **Vale-Presente — labels corrigidos**: "Formato" → "Tipo de entrega" (sheet + workbench); "Canal digital" → "Via (email ou WhatsApp)"; "Para quem entregar?" → "Para quem enviar?"; "Preservação agendada?" → "Estado de utilização". Adicionadas validações obrigatórias nos campos de entrega (`delivery_recipient`, `delivery_format`, `delivery_channel` quando digital). (2) **Cards — foto ocultada**: grupos Sem resposta, Pré-reservas, Reservas e Cancelamentos não mostram a área de foto (prop `showPhoto={false}`); urgência e loading passam a mostrar-se inline no corpo do card quando sem foto. (3) **50×70 cor**: mudado de `rose` (parecia erro) para `indigo` (progressão visual sky→violet→indigo com o tamanho). (4) **Datas**: corrigido formato "dd MMM yyyy, HH:mm" para "dd/MM/yyyy, HH:mm" no workbench da Preservação (linhas de metadata criada/actualizada). Regra guardada em memória: todas as datas usam dd/MM/yyyy; timestamps usam dd/MM/yyyy, HH:mm.
- **2026-05-07 (sessão 18):** Fase 3 — Vale-Presente (admin) construída. Migração `009_create_vouchers.sql` cria a tabela `vouchers` com remetente (nome/contacto/email/tel), o vale (destinatário/mensagem/valor com `CHECK >= 300`), entrega (`delivery_recipient`, `delivery_format` digital/físico, `delivery_channel`, `delivery_shipping_cost`), estado admin (`payment_status` com só 2 valores: `100_pago`/`100_por_pagar`; `send_status`: enviado/agendado/nao_agendado; `usage_status`: preservacao_agendada/preservacao_nao_agendada), validade (`expiry_date` default `+2 years`), NIF/fatura. Função `generate_voucher_code()` gera códigos de 6 caracteres alfanuméricos sem `0`,`O`,`I`,`1` (mais legível); trigger `set_voucher_code` preenche-os automaticamente no INSERT com retry até 10x para garantir unicidade. RLS espelha o padrão das encomendas (admins escrevem, Ana lê). Tipos em `src/types/voucher.ts`; helpers em `src/lib/supabase/vouchers.ts` (`groupVouchers`, `isExpiringSoon`, `isExpired`, `monthsUntilExpiry`). **Listagem (`/vale-presente`)**: tabela agrupada Pré-reservas (100% por pagar) / Reservas (100% pago); selects inline coloridos para pagamento, envio e utilização; coluna de validade com aviso visual (vermelho se expirado, âmbar se ≤3 meses); empty state ilustrado; alerta no topo a contar vales pagos sem preservação agendada a expirar nos próximos 3 meses; pesquisa por nome/código/email; modo leitura para a Ana. **Sheet "Novo vale"**: validação mín. 300€, validações remetente/destinatário, "florista" obrigatória, defaults sensatos. **Workbench (`/vale-presente/[code]`)**: 3 colunas (esquerda=hero+remetente+origem, meio=vale+entrega+comentários, direita=pagamento+envio+utilização+fatura+metadata); auto-save por campo no blur; diálogo de confirmação ao marcar como pago a lembrar comprovativo + perguntar NIF; botão "+2 anos" para reset rápido da validade; arquivar (soft delete); rota aceita o código curto (6 dig) ou o UUID interno. Build OK, sem erros TypeScript.
- **2026-05-04 (sessão 17):** Pagamento. (1) Removido o estado `30_por_pagar` — funcionalmente equivalente a `70_pago` (70% pago = 30% por pagar). Tipo `PaymentStatus`, `PAYMENT_STATUS_LABELS`, `PAYMENT_COLORS` (em `_styles.ts` e no `workbench-client.tsx` local) e CHECK constraint da BD limpos. Migração `008_remove_30_por_pagar.sql` converte registos existentes (UPDATE → `70_pago`) antes de re-emitir o constraint. (2) `70_pago` e `30_pago` ganharam cores distintas (antes eram ambas amarelas, ilegíveis): gradiente `red → amber → lime → green` consoante a quantia já paga (100% por pagar = vermelho, 30% pago = âmbar, 70% pago = lime, 100% pago = verde). `PAYMENT_DOT_COLORS` ajustado em conformidade.
- **2026-05-04 (sessão 16):** Pequenas afinações da listagem de Preservação. (1) **Ordenação**: encomendas em todas as vistas (tabela, cards, calendário, timeline) ordenadas por data do evento ascendente — mais próxima primeiro; encomendas sem data ficam no fim. Sort feito tanto no `page.tsx` (Supabase `order("event_date", asc, nullsFirst:false)`) como no `groupOrders` (defesa em profundidade) via helper `byEventDateAsc`. (2) **Mover manualmente para Sem resposta**: nova coluna `manually_no_response BOOLEAN DEFAULT false` (migração `007_manually_no_response.sql`). `isWithoutResponse` retorna true se a flag estiver activa OU se passaram ≥4 dias sem contacto (regra automática mantida). Botão **"Sem resposta"** (ícone Clock, vermelho) aparece nas linhas de Pré-reservas que ainda não estão automaticamente sinalizadas. Botão **"Pré-reservas"** (ícone Undo2) aparece nas linhas de Sem resposta marcadas manualmente desde que ainda não tenham passado 4 dias — clarear a flag não tem efeito visível se a regra automática já se aplicar. "Marcar contactada" agora também limpa a flag manual. Apenas admin vê os botões.
- **2026-05-04 (sessão 15):** Importação das encomendas históricas do Monday. Script gerador `scripts/import-monday.js` lê `public/mondayexport.xlsx` e produz `supabase/migrations/006_import_monday.sql` com 17 INSERTs. Excluídas: "Teste Noiva teste", João Correia, Sandra Carvalho (estes dois últimos já metidos manualmente). Decisões: telemóveis convertidos de notação científica para string de dígitos; "Sinal por pagar"/"N/A" → `100_por_pagar`; Laureana e Eugenia forçadas para `cancelado` (deram gosto antes de pagar); IDs do Sheets reaproveitados como `order_id` quando existem; cupões antigos curtos (`F2B5R`, `F2B5R2`, `F2B6R1`) mantidos como estão por terem sido enviados aos clientes. **Nova regra para cupões:** alfabeto sem `0` nem `O` (evita confusão na leitura) — extraído para `src/lib/coupon.ts` e usado em `actions.ts` e `lib/supabase/orders.ts`. **Florista obrigatória:** campo "Que florista?" aparece e é validado em `nova-encomenda-sheet.tsx` quando how_found_fbr=florista; também aparece (sem validação dura) no workbench. Falta replicar no formulário público (Fase 5).
- **2026-05-02 (sessão 1):** Leitura do PDF spec. Plano por fases definido.
- **2026-05-02 (sessão 2):** Fase 1 completa. Login Netflix com fotos a funcionar no Vercel. Mudámos de Google OAuth para email+password com subendereços Gmail. Deploy Vercel configurado com env vars.
- **2026-05-02 (sessão 3):** Fase 2 iniciada. Schema BD criado e migrado no Supabase. Tabela de encomendas com grupos colapsáveis a funcionar. Formulário "Nova Encomenda" funcional. Corrigido 403 (mudança para Server Component + Server Actions). Deploy OK.
- **2026-05-03 (sessão 4):** Corrigido erro 500 no formulário "Nova Encomenda". Causa: role `authenticated` sem EXECUTE em `generate_order_id()` e sem INSERT em `audit_log`. Fix em `003_fix_permissions.sql` (executado no Supabase SQL Editor). Criação de encomendas a funcionar no Vercel.
- **2026-05-03 (sessão 5):** Workbench redesenhado para cobrir toda a spec. Adicionados: hero com foto da encomenda (placeholder elegante quando vazia), atalhos rápidos (Drive, página pública de status, copiar ID), bloco Extras no quadro (opções + texto livre), Peças extra (mini-quadros, ornamentos, pendentes com qty condicional), Galeria de inspiração (add/remove de URLs com preview), placeholders para Comunicações Gmail/WhatsApp e Assistente IA, NIF + anexo de fatura condicional, parceiro recomendador (select desactivado a aguardar aba Parcerias), alerta visual de fatura em falta, contagem decrescente até evento.
- **2026-05-03 (sessão 6):** Tabela de encomendas com edição inline. (1) Coluna Estado é agora um dropdown clicável que muda o estado sem abrir o workbench. (2) Pré-reservas mostram botão "Marcar contactada" rápido na linha; quando contactadas mostram badge verde. (3) Workbench com diálogo de confirmação ao mudar pagamento para qualquer estado pago — lembra anexar comprovativo à Drive e pergunta se cliente pediu NIF (com input do NIF inline). Confirmado que todos os ficheiros vão para a Drive e não para Supabase Storage.
- **2026-05-03 (sessão 7):** Workbench reorganizado para 3 colunas. **Esquerda (sticky)**: Comunicações (tabs Email/WhatsApp coloridos) + Assistente IA — visíveis logo ao abrir a encomenda. **Meio**: hero com foto vertical 3:4 + sumário + atalhos (Drive, status público, copiar ID), seguido de Cliente, Evento, Flores, Extras, Peças extra, Galeria, Origem. **Direita**: Finanças, Parceria, Entrega, Cupão, Metadata. Removida a caixa "Pasta Google Drive" duplicada — edição via popover no atalho do hero. Adicionada paleta de cores discretas (borda esquerda + ícone) por secção: rose/amber/emerald/orange/indigo/pink/slate/green/sky/purple/yellow/violet/blue. Guardada na memória a decisão de auto-criar pasta Drive ao 1º pagamento (Fase 6).
- **2026-05-03 (sessão 8):** Vista "Workbench" da listagem trocada por **Cards** (foto + nome + data); workbench dedicado fica só ao clicar numa encomenda. URL do workbench passa a usar `order_id` (curto) em vez do UUID — `/preservacao/<order_id>`; UUID antigo continua a resolver, por compatibilidade. Lembrete (diálogo) ao mudar estado para *Quadro recebido* a pedir data de entrega. Campo "Código vale-presente" só aparece quando `how_found_fbr = vale_presente`. Card "Cupão 5%" passa a estar sempre visível (com mensagem quando o código ainda não foi gerado). "Cliente pediu fatura com NIF" trocado de checkbox para Select **Sim/Não** (workbench + diálogo de pagamento). Botão "Copiar ID" redundante removido do hero (mantido o do header, ao clicar no `#…`). Layout admin passa de `min-h-screen` para `h-screen overflow-hidden` para a parte de baixo da sidebar (perfil, sair, theme, colapsar) ficar sempre visível mesmo em páginas longas como o workbench.
- **2026-05-03 (sessão 9):** Bug visual: o `SelectValue` (base-ui, não Radix) mostrava o valor cru do enum (`quadro_recebido`, `nao_disse_nada`…) em vez do texto do `SelectItem`. Corrigido com prop `labels` no `SelectValue` que mapeia valor → label legível. Adicionados mapas de labels em `database.ts` para todos os enums (contacto, eventos, envio, fundo, tamanho, sim/não, origem, comissão, cupão, feedback, idioma) e aplicados em todos os `<Select>` da Preservação (tabela, workbench, diálogos, formulário Nova encomenda).
- **2026-05-03 (sessão 10):** Tabela de encomendas redesenhada com colunas adaptadas ao ciclo de vida. Colunas fixas: Cliente · Data evento · Localização · Estado · Orçamento · Pagamento. Coluna de logística condicional: **Envio das flores** nos grupos Pré-reservas/Sem resposta/Reservas/Cancelamentos (como as flores chegam à FBR), **Receção do quadro** nos grupos Preservação e design/Finalização/Concluídos (como o quadro chega ao cliente). Removida a coluna ID e o tipo de evento simplificado para subtítulo cinzento da célula Cliente.
- **2026-05-04 (sessão 12):** Adicionadas vistas **Calendário** e **Timeline** à listagem de encomendas. Constantes de cor/ícone/grupos de estado extraídas para `_styles.ts` (partilhadas entre tabela, cards, calendário e timeline). **Calendário** (`calendar-view.tsx`): grelha mensal de Seg-Dom com navegação prev/next + botão "Hoje", máximo 3 eventos por dia (com "+N mais" expansível), bolinha colorida por estado, click no evento abre o workbench, legenda compacta no rodapé. **Timeline** (`timeline-view.tsx`): eventos agrupados por mês, secção "Próximos eventos" sempre aberta (asc), "Eventos passados" colapsada por defeito (desc), secção "Sem data de evento" para encomendas sem `event_date`. Cada linha: data em bloco (Mês/Dia/Dia-da-semana), nome+tipo, badge urgente vermelho se ≤5 dias, localização com ícone, badge de estado. Ordem dos botões do switcher: Tabela · Cards · Calendário · Timeline.
- **2026-05-04 (sessão 14):** Permissões admin/viewer. Helpers `src/lib/auth/roles.ts` (mapeamento email → papel; partilhado servidor + cliente) e `src/lib/auth/server.ts` (`getCurrentRole()`, `requireAdmin()` — atira erro em quem não é admin). Todas as Server Actions de Preservação (`createOrderAction`, `updateOrderAction`, `deleteOrderAction`) e Status (`updateOrderPublicStatusAction`, `updateDefaultMessagesAction`) começam por `await requireAdmin()`. Páginas de servidor passam `canEdit` aos clientes. **Preservação:** botão "Nova encomenda" escondido para viewer; `StatusSelect` e `PaymentSelect` ganharam prop `disabled`; "Marcar contactada" escondida; restantes vistas (cards, calendário, timeline) já eram só leitura. **Workbench:** banner âmbar "Modo leitura" no topo + `<fieldset disabled className="contents">` à volta do corpo todo (header + 3 colunas + dialogs) — todos os inputs/buttons/selects nativos ficam disabled em cascata, links de navegação (`<a>`) continuam clicáveis. **Status:** mesmo banner; `Mensagens default` button escondido; `StatusRow` desabilita o select de idioma, o input de data e os botões PT/EN. **Mensagens default:** banner + fieldset disabled à volta do formulário inteiro. **Sidebar:** debaixo do nome aparece o badge "Visualizador" (só para a Ana). Por defeito, qualquer email desconhecido cai em viewer (defesa em profundidade).
- **2026-05-04 (sessão 13):** Aba **Status** completa. Mapeamento estado interno → fase pública (12 fases: 0 pré-timeline + 1-11 visíveis + "cancelada" à parte). Decisão: `cancelado` interno mostra "Cancelada" pública (e NÃO regride a fase 0 como o PDF sugeria) — evita que o link do cliente que estava em "A ser emoldurado" pareça regredir. Mensagens default extraídas do PDF "plataforma admin" (PT + EN para todas as fases). Migração 005 adiciona `public_status_message_pt/en`, `public_status_language` (pt/en/ambos), `estimated_delivery_date`, `public_status_updated_at`, trigger `sync_public_status_fields` (data prevista = +6 meses quando passa a `flores_na_prensa`; bumpa `public_status_updated_at` quando algo público muda) e tabela singleton `public_status_settings` para mensagens default editáveis. Lib `src/lib/public-status.ts` com mapeamento, labels PT/EN, cores e helpers (`resolveMessage`, `publicStatusUrl`). Página `/status`: tabela com filtro por fase, search, toggle esconder concluídas/canceladas, edição inline (idioma, data prevista) e diálogo de edição de mensagem (PT + EN, mostra preview do default, botão "voltar ao default"). Página `/status/mensagens-default`: editor global das mensagens default (rephrase global, encomendas com mensagem personalizada não são afectadas). Workbench: novo card "Status público" na coluna direita (mostra fase pública + idioma + data prevista + link para editar mensagem na aba Status); URL pública corrigida para `https://status.floresabeirario.pt/<order_id>` (sem `?id=`). Memória atualizada com a coexistência Excel↔plataforma durante a migração.
- **2026-05-04 (sessão 11):** Pacote grande de afinações pós-uso. **Tabela:** pagamento agora editável inline (mesmo padrão do estado). Botão de **Export CSV** (com `;` + BOM, abre directamente no Excel pt-PT). Indicador visual e spinner na linha/card clicado para evitar duplo-clique enquanto o workbench carrega. Adicionado `loading.tsx` no `[id]` para fallback global. **Workbench:** dropdown de estados redesenhado com grupos por fase + separadores + ponto colorido por estado + cores fortes no trigger; Pagamento inline na coluna direita; **cores associadas** a cada estado de Comissão, Cupão e Feedback (badge no item do select e fundo no trigger). Cards consolidados: Cliente + Evento agora vivem na coluna direita do hero (mostra meses+dias até ao evento via `relativeMonthsDays`). “Contacto preferido” mudou-se para o card Comunicações, junto a links rápidos de email/WhatsApp. Galeria de inspiração mudou para a coluna esquerda. Flores + Extras + Peças extra estão num único card; Envio das flores + Receção do quadro num card separado, com o `Pago?` agora Select Sim/Não escondido quando o método é `Em mãos`. **Origem:** se `Outro`, aparece campo livre `how_found_fbr_other` (nova coluna SQL — migração 004); cada plataforma tem cor própria (`HOW_FOUND_FBR_COLORS`). **NIF** apresenta-se à direita do Sim quando aplicável (workbench + diálogo de pagamento). **Cupão 5%:** input editável (encomendas antigas) + botão `+2 anos` que preenche a validade. **ID da encomenda:** editável via popover no header. **Fotos:** novo helper `lib/drive-url.ts` converte links partilháveis do Drive para `lh3.googleusercontent.com/d/<id>` para que o `<img>` os mostre — usado nas cards e no hero. **Extras** novas opções (`Não pretendo incluir extras`, `Votos manuscritos`, `Convite do casamento`, `Fitas, tecidos ou rendas`, `Fotografia`, `Joia ou medalha`, `Coleira de animal`, `Cartas ou bilhetes`, `Outro (especifique abaixo)`); textarea de detalhe só aparece quando `Outro` está marcado, e `Não pretendo incluir extras` é mutuamente exclusivo.
